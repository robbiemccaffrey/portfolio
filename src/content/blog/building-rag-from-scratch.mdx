---
title: "Building RAG From Scratch: What I Learned"
date: 2026-03-05
description: "A practical guide to building retrieval-augmented generation — embeddings, chunking, vector stores, and the retrieval pipeline that ties it all together."
tags: ["ai", "rag", "agents", "llm"]
draft: true
---

## Why RAG

Large language models are impressive, but they have two fundamental problems when you try to use them on your own data. First, they have a **knowledge cutoff** — they don't know about anything that happened after their training data was collected. Second, they have **context limits** — even with long-context models, you can't just dump your entire knowledge base into a prompt and hope for the best.

**Retrieval-augmented generation (RAG)** solves both of these. Instead of relying on what the model already knows, you retrieve relevant documents at query time and inject them into the prompt as context. The model then generates a response grounded in your actual data rather than its training set.

Here's the full flow at a glance:

<div class="not-prose my-10">
  <div class="rounded-xl border border-gray-200 dark:border-gray-700 overflow-hidden">
    <div class="px-5 py-2.5 bg-gray-50 dark:bg-gray-800/50 border-b border-gray-200 dark:border-gray-700 font-sans text-xs font-medium uppercase tracking-wider text-gray-500 dark:text-gray-400">How RAG works</div>
    <div class="px-4 py-6 sm:px-8 sm:py-8">
      <svg viewBox="0 0 720 310" class="w-full" aria-label="Diagram showing how RAG works: a user question is embedded and used to search a vector store, the matching documents are combined with the original question and sent to an LLM, which produces a grounded answer.">
        {/* ---- Step 1: User question ---- */}
        <rect x="10" y="80" width="140" height="60" rx="10" class="fill-blue-50 dark:fill-blue-900/30 stroke-blue-300 dark:stroke-blue-700" stroke-width="1.5"/>
        <text x="80" y="106" text-anchor="middle" class="fill-blue-700 dark:fill-blue-300" font-size="11" font-family="system-ui" font-weight="600">User question</text>
        <text x="80" y="122" text-anchor="middle" class="fill-blue-500 dark:fill-blue-400" font-size="9" font-family="system-ui" font-style="italic">"How do I reset my</text>
        <text x="80" y="133" text-anchor="middle" class="fill-blue-500 dark:fill-blue-400" font-size="9" font-family="system-ui" font-style="italic">password?"</text>
        <text x="80" y="68" text-anchor="middle" class="fill-gray-400 dark:fill-gray-500" font-size="10" font-family="system-ui" font-weight="700">1</text>
        {/* Arrow from question to vector store */}
        <defs>
          <marker id="rag-arrow" markerWidth="8" markerHeight="6" refX="8" refY="3" orient="auto"><polygon points="0 0, 8 3, 0 6" class="fill-gray-400 dark:fill-gray-500"/></marker>
        </defs>
        <line x1="150" y1="105" x2="198" y2="105" class="stroke-gray-300 dark:stroke-gray-600" stroke-width="1.5" marker-end="url(#rag-arrow)"/>
        <text x="174" y="98" text-anchor="middle" class="fill-gray-400 dark:fill-gray-500" font-size="8" font-family="system-ui">embed</text>
        {/* ---- Step 2: Vector store ---- */}
        <rect x="205" y="40" width="160" height="140" rx="10" class="fill-amber-50 dark:fill-amber-900/20 stroke-amber-300 dark:stroke-amber-700" stroke-width="1.5"/>
        <text x="285" y="65" text-anchor="middle" class="fill-amber-700 dark:fill-amber-300" font-size="11" font-family="system-ui" font-weight="600">Vector Store</text>
        {/* Mini document rows */}
        <rect x="220" y="77" width="130" height="22" rx="4" class="fill-emerald-100 dark:fill-emerald-900/30 stroke-emerald-300 dark:stroke-emerald-600" stroke-width="1"/>
        <text x="228" y="92" class="fill-emerald-700 dark:fill-emerald-300" font-size="8" font-family="system-ui">password-reset.md</text>
        <text x="340" y="92" text-anchor="end" class="fill-emerald-500 dark:fill-emerald-400" font-size="7" font-family="monospace">0.94</text>
        <rect x="220" y="103" width="130" height="22" rx="4" class="fill-emerald-50 dark:fill-emerald-900/15 stroke-emerald-200 dark:stroke-emerald-700" stroke-width="1"/>
        <text x="228" y="118" class="fill-emerald-600 dark:fill-emerald-400" font-size="8" font-family="system-ui">account-security.md</text>
        <text x="340" y="118" text-anchor="end" class="fill-emerald-500 dark:fill-emerald-400" font-size="7" font-family="monospace">0.82</text>
        <rect x="220" y="129" width="130" height="22" rx="4" class="fill-gray-50 dark:fill-gray-800/40 stroke-gray-200 dark:stroke-gray-700" stroke-width="1"/>
        <text x="228" y="144" class="fill-gray-400 dark:fill-gray-500" font-size="8" font-family="system-ui">billing-faq.md</text>
        <text x="340" y="144" text-anchor="end" class="fill-gray-400 dark:fill-gray-500" font-size="7" font-family="monospace">0.31</text>
        <text x="285" y="168" text-anchor="middle" class="fill-gray-400 dark:fill-gray-500" font-size="8" font-family="system-ui" font-style="italic">similarity search</text>
        <text x="285" y="28" text-anchor="middle" class="fill-gray-400 dark:fill-gray-500" font-size="10" font-family="system-ui" font-weight="700">2</text>
        {/* Arrow from vector store to prompt assembly */}
        <line x1="365" y1="95" x2="413" y2="95" class="stroke-gray-300 dark:stroke-gray-600" stroke-width="1.5" marker-end="url(#rag-arrow)"/>
        <text x="389" y="88" text-anchor="middle" class="fill-gray-400 dark:fill-gray-500" font-size="8" font-family="system-ui">top K</text>
        {/* ---- Step 3: Prompt assembly ---- */}
        <rect x="420" y="20" width="140" height="180" rx="10" class="fill-violet-50 dark:fill-violet-900/20 stroke-violet-300 dark:stroke-violet-700" stroke-width="1.5"/>
        <text x="490" y="45" text-anchor="middle" class="fill-violet-700 dark:fill-violet-300" font-size="11" font-family="system-ui" font-weight="600">Prompt</text>
        {/* Context block */}
        <rect x="434" y="55" width="112" height="52" rx="4" class="fill-emerald-50 dark:fill-emerald-900/20 stroke-emerald-200 dark:stroke-emerald-700" stroke-width="1" stroke-dasharray="4 2"/>
        <text x="440" y="68" class="fill-emerald-600 dark:fill-emerald-400" font-size="8" font-family="system-ui" font-weight="600">Context:</text>
        <text x="440" y="80" class="fill-gray-500 dark:fill-gray-400" font-size="7.5" font-family="system-ui">To reset your password,</text>
        <text x="440" y="90" class="fill-gray-500 dark:fill-gray-400" font-size="7.5" font-family="system-ui">go to Settings → Security</text>
        <text x="440" y="100" class="fill-gray-500 dark:fill-gray-400" font-size="7.5" font-family="system-ui">→ Change password…</text>
        {/* Question block */}
        <rect x="434" y="113" width="112" height="36" rx="4" class="fill-blue-50 dark:fill-blue-900/20 stroke-blue-200 dark:stroke-blue-700" stroke-width="1" stroke-dasharray="4 2"/>
        <text x="440" y="126" class="fill-blue-600 dark:fill-blue-400" font-size="8" font-family="system-ui" font-weight="600">Question:</text>
        <text x="440" y="138" class="fill-gray-500 dark:fill-gray-400" font-size="7.5" font-family="system-ui">How do I reset my password?</text>
        {/* System instruction */}
        <text x="490" y="167" text-anchor="middle" class="fill-violet-400 dark:fill-violet-500" font-size="7" font-family="system-ui" font-style="italic">"Answer using only the</text>
        <text x="490" y="177" text-anchor="middle" class="fill-violet-400 dark:fill-violet-500" font-size="7" font-family="system-ui" font-style="italic">provided context."</text>
        <text x="490" y="8" text-anchor="middle" class="fill-gray-400 dark:fill-gray-500" font-size="10" font-family="system-ui" font-weight="700">3</text>
        {/* Arrow from prompt to LLM */}
        <line x1="560" y1="110" x2="598" y2="110" class="stroke-gray-300 dark:stroke-gray-600" stroke-width="1.5" marker-end="url(#rag-arrow)"/>
        {/* ---- Step 4: LLM ---- */}
        <rect x="605" y="70" width="100" height="80" rx="10" class="fill-gray-100 dark:fill-gray-800 stroke-gray-300 dark:stroke-gray-600" stroke-width="1.5"/>
        <text x="655" y="102" text-anchor="middle" class="fill-gray-700 dark:fill-gray-200" font-size="13" font-family="system-ui" font-weight="700">LLM</text>
        <text x="655" y="118" text-anchor="middle" class="fill-gray-400 dark:fill-gray-500" font-size="8" font-family="system-ui">generates answer</text>
        <text x="655" y="58" text-anchor="middle" class="fill-gray-400 dark:fill-gray-500" font-size="10" font-family="system-ui" font-weight="700">4</text>
        {/* ---- Step 5: Grounded answer ---- */}
        <line x1="655" y1="150" x2="655" y2="188" class="stroke-gray-300 dark:stroke-gray-600" stroke-width="1.5" marker-end="url(#rag-arrow)"/>
        <rect x="580" y="195" width="150" height="66" rx="10" class="fill-emerald-50 dark:fill-emerald-900/20 stroke-emerald-300 dark:stroke-emerald-600" stroke-width="1.5"/>
        <text x="655" y="218" text-anchor="middle" class="fill-emerald-700 dark:fill-emerald-300" font-size="10" font-family="system-ui" font-weight="600">Grounded answer</text>
        <text x="655" y="234" text-anchor="middle" class="fill-gray-500 dark:fill-gray-400" font-size="8" font-family="system-ui">"Go to Settings → Security</text>
        <text x="655" y="245" text-anchor="middle" class="fill-gray-500 dark:fill-gray-400" font-size="8" font-family="system-ui">→ Change password."</text>
        <text x="655" y="188" text-anchor="middle" class="fill-gray-400 dark:fill-gray-500" font-size="10" font-family="system-ui" font-weight="700">5</text>
        {/* ---- Step labels along the bottom ---- */}
        <text x="80" y="300" text-anchor="middle" class="fill-gray-400 dark:fill-gray-500" font-size="8" font-family="system-ui">User asks</text>
        <text x="285" y="300" text-anchor="middle" class="fill-gray-400 dark:fill-gray-500" font-size="8" font-family="system-ui">Find similar docs</text>
        <text x="490" y="300" text-anchor="middle" class="fill-gray-400 dark:fill-gray-500" font-size="8" font-family="system-ui">Build prompt</text>
        <text x="655" y="300" text-anchor="middle" class="fill-gray-400 dark:fill-gray-500" font-size="8" font-family="system-ui">Answer with citations</text>
        <text x="80" y="288" text-anchor="middle" class="fill-blue-500 dark:fill-blue-400" font-size="9" font-family="system-ui" font-weight="700">①</text>
        <text x="285" y="288" text-anchor="middle" class="fill-amber-500 dark:fill-amber-400" font-size="9" font-family="system-ui" font-weight="700">②</text>
        <text x="490" y="288" text-anchor="middle" class="fill-violet-500 dark:fill-violet-400" font-size="9" font-family="system-ui" font-weight="700">③④</text>
        <text x="655" y="288" text-anchor="middle" class="fill-emerald-500 dark:fill-emerald-400" font-size="9" font-family="system-ui" font-weight="700">⑤</text>
      </svg>
    </div>
  </div>
</div>

The user's question gets embedded into a vector and used to search a store of pre-indexed documents. The most relevant chunks come back ranked by similarity. Those chunks, along with the original question, get assembled into a prompt with instructions to only use the provided context. The LLM generates a response grounded in your actual data — not its training set.

The rest of this post walks through each piece: how embeddings work, how to chunk your documents, how vector stores search and rank, and how the retrieval pipeline ties it all together. This is what I wish someone had told me when I started learning about it.

---

## Embeddings

Before you can retrieve anything, you need a way to represent text as something a computer can compare. That's what **embeddings** are — numerical vectors that capture the semantic meaning of a piece of text. Two sentences that mean similar things will have vectors that are close together in high-dimensional space, even if they use completely different words.

You generate embeddings by passing text through an embedding model. The output is a fixed-length array of floats — typically 256 to 3,072 dimensions depending on the model. Here's what one actually looks like:

<div class="not-prose my-8 rounded-xl border border-gray-200 dark:border-gray-700 overflow-hidden text-sm font-mono">
  <div class="px-5 py-3 bg-gray-50 dark:bg-gray-800/50 border-b border-gray-200 dark:border-gray-700 text-gray-500 dark:text-gray-400 font-sans text-xs font-medium uppercase tracking-wider">Input</div>
  <div class="px-5 py-4 text-gray-900 dark:text-white">"How do I reset my password?"</div>
  <div class="px-5 py-1 text-center text-gray-400 dark:text-gray-500 text-lg">↓ <span class="text-xs font-sans align-middle">text-embedding-3-small</span></div>
  <div class="px-5 py-3 bg-gray-50 dark:bg-gray-800/50 border-t border-gray-200 dark:border-gray-700 text-gray-500 dark:text-gray-400 font-sans text-xs font-medium uppercase tracking-wider">Embedding — 1,536 dimensions</div>
  <div class="px-5 py-4 text-blue-600 dark:text-blue-400 overflow-x-auto whitespace-nowrap">
    [<span class="text-gray-900 dark:text-white">0.0231</span>, <span class="text-gray-900 dark:text-white">-0.0892</span>, <span class="text-gray-900 dark:text-white">0.1345</span>, <span class="text-gray-900 dark:text-white">0.0012</span>, <span class="text-gray-900 dark:text-white">-0.0567</span>, <span class="text-gray-900 dark:text-white">0.0891</span>, <span class="text-gray-400 dark:text-gray-500">… 1,530 more …</span>, <span class="text-gray-900 dark:text-white">-0.0234</span>]
  </div>
</div>

Each number represents some learned aspect of the text's meaning. Individually they're not interpretable, but the vector as a whole places the text at a specific point in high-dimensional space. Texts with similar meanings end up near each other — "reset my password" and "change my login credentials" would have vectors pointing in nearly the same direction, even though the words are different.

The model you choose matters. **OpenAI's `text-embedding-3-small`** is a solid default — it's cheap, fast, produces 1,536-dimension vectors, and performs well on most retrieval benchmarks. If you need better accuracy and don't mind the cost, `text-embedding-3-large` (3,072 dimensions) is worth testing. On the open-source side, models from the **Sentence Transformers** family (like `all-MiniLM-L6-v2`, 384 dimensions) are free and run locally, which matters if you have data residency constraints.

> **One important thing:** once you pick an embedding model, you're committed to it. Your queries and your stored documents need to use the same model. Switching later means re-embedding your entire corpus.

---

## Chunking

You can't just embed an entire 50-page document as a single vector. The embedding would be too diluted — it'd capture the general theme but lose the specific details you actually need to retrieve. So you **chunk** your documents into smaller pieces before embedding them.

This sounds simple. It isn't. Chunking strategy has a bigger impact on retrieval quality than almost any other decision you'll make.

### Chunking strategies

**Fixed-size chunking** is the simplest approach — split every N characters or tokens. It's fast and predictable, but it splits mid-sentence and ignores document structure. You'll get chunks that start halfway through a paragraph and end in the middle of a code block.

**Recursive character splitting** is a better default. It tries to split on paragraph boundaries first, then sentences, then words, only falling back to character-level splits when necessary. It respects the natural structure of text.

**Semantic chunking** groups text by meaning — it embeds individual sentences and creates chunk boundaries where the embedding similarity drops. It's more expensive to compute but produces chunks that are semantically coherent.

### Chunk size matters — a lot

A typical starting point is **256–512 tokens** per chunk. That's roughly a paragraph or two of text. But the right size depends entirely on what you're building.

**Smaller chunks (128–256 tokens)** give you more precise retrieval — each chunk covers one specific idea, so the vector representation is focused. The trade-off is that you lose surrounding context. These work well for **FAQ systems** where each question-answer pair is a natural, self-contained unit. If someone asks "How do I reset my password?", you want to retrieve exactly the chunk that answers that question, not a chunk that also covers account deletion and billing changes.

**Larger chunks (512–1,024 tokens)** give the model more context per retrieval hit but produce less targeted vectors. These work better for **technical documentation** where a code example might not make sense without the explanation above it, or where a concept spans several paragraphs. If you're indexing API docs and someone asks how to authenticate, you probably want the chunk to include the explanation, the code snippet, *and* the common error cases — not just the code block alone.

The key insight is that **your chunk boundaries should align with the natural units of meaning in your content.** For FAQs, a chunk is a question-answer pair. For documentation, a chunk might be an entire subsection including its code examples. For legal documents, a chunk might be a single clause. Don't just pick a number and split blindly — think about what a useful retrieval result looks like for your specific use case, and chunk accordingly.

### Overlap

**Overlap** means that consecutive chunks share some text at their boundaries — typically 10–20% of the chunk size. This is critical because without it, important information that happens to fall at a chunk boundary gets split across two vectors and may not be retrieved by either one.

<div class="not-prose my-8 space-y-6">
  <div>
    <div class="text-xs font-semibold uppercase tracking-wider text-red-600 dark:text-red-400 mb-3">Without overlap — context lost at boundary</div>
    <div class="flex gap-0">
      <div class="flex-1 rounded-l-lg border border-r-0 border-gray-200 dark:border-gray-700 bg-gray-50 dark:bg-gray-800/50 p-4">
        <div class="text-xs font-semibold text-gray-400 dark:text-gray-500 mb-2">Chunk 1</div>
        <p class="text-sm text-gray-700 dark:text-gray-300 leading-relaxed">The API uses bearer tokens for auth. Tokens</p>
      </div>
      <div class="flex-1 rounded-r-lg border border-gray-200 dark:border-gray-700 bg-gray-50 dark:bg-gray-800/50 p-4">
        <div class="text-xs font-semibold text-gray-400 dark:text-gray-500 mb-2">Chunk 2</div>
        <p class="text-sm text-gray-700 dark:text-gray-300 leading-relaxed">must be refreshed every 30 minutes. Use the /refresh…</p>
      </div>
    </div>
    <p class="text-xs text-red-600 dark:text-red-400 mt-2 text-center">"Tokens must be refreshed every 30 minutes" is split across chunks — neither chunk contains the full sentence</p>
  </div>
  <div>
    <div class="text-xs font-semibold uppercase tracking-wider text-emerald-600 dark:text-emerald-400 mb-3">With overlap — context preserved</div>
    <div class="space-y-0">
      <div class="rounded-t-lg border border-gray-200 dark:border-gray-700 bg-gray-50 dark:bg-gray-800/50 p-4">
        <div class="text-xs font-semibold text-gray-400 dark:text-gray-500 mb-2">Chunk 1</div>
        <p class="text-sm text-gray-700 dark:text-gray-300 leading-relaxed">The API uses bearer tokens for auth. <span class="bg-emerald-100 dark:bg-emerald-900/40 text-emerald-800 dark:text-emerald-300 px-1 rounded">Tokens must be refreshed every 30 minutes.</span></p>
      </div>
      <div class="flex items-center justify-center -my-3 relative z-10">
        <span class="text-xs font-semibold bg-emerald-100 dark:bg-emerald-900/40 text-emerald-700 dark:text-emerald-300 px-3 py-1 rounded-full">overlap</span>
      </div>
      <div class="rounded-b-lg border border-t-0 border-gray-200 dark:border-gray-700 bg-gray-50 dark:bg-gray-800/50 p-4">
        <div class="text-xs font-semibold text-gray-400 dark:text-gray-500 mb-2">Chunk 2</div>
        <p class="text-sm text-gray-700 dark:text-gray-300 leading-relaxed"><span class="bg-emerald-100 dark:bg-emerald-900/40 text-emerald-800 dark:text-emerald-300 px-1 rounded">Tokens must be refreshed every 30 minutes.</span> Use the /refresh endpoint to obtain a new token…</p>
      </div>
    </div>
    <p class="text-xs text-emerald-600 dark:text-emerald-400 mt-2 text-center">Both chunks contain the complete sentence — either one will match a relevant query</p>
  </div>
</div>

This matters more for narrative content — documentation, reports, articles — where ideas flow across paragraphs. For content with clear boundaries like FAQs, overlap is less important because each chunk is already self-contained.

### Putting it together

In practice, **recursive character splitting with overlap** is a good starting point:

```python
from langchain.text_splitter import RecursiveCharacterTextSplitter

splitter = RecursiveCharacterTextSplitter(
    chunk_size=512,       # ~1-2 paragraphs
    chunk_overlap=64,     # ~12% overlap
    separators=["\n\n", "\n", ". ", " ", ""],
)

chunks = splitter.split_text(document_text)

# Preserve source metadata on each chunk
chunk_records = [
    {
        "text": chunk,
        "source": document_id,
        "chunk_index": i,
    }
    for i, chunk in enumerate(chunks)
]
```

But treat this as a starting point, not a final answer. Swap in larger chunks for documentation, smaller ones for FAQs, and always test retrieval quality with real queries before committing to a chunk size.

**Preserve metadata.** Every chunk should carry a reference back to its source document, its position, and any relevant tags. You'll need this for citation, filtering, and debugging retrieval issues later.

---

## Vector Stores

Once you have embeddings, you need somewhere to store them and a way to search them efficiently. That's what a **vector store** does — it indexes your embeddings and supports similarity search, returning the vectors closest to a given query vector.

Here's what a row in a vector store actually looks like — it's just your text, some metadata, and that big array of floats sitting alongside it:

<div class="not-prose my-8 rounded-xl border border-gray-200 dark:border-gray-700 overflow-hidden text-sm">
  <div class="px-5 py-2.5 bg-gray-50 dark:bg-gray-800/50 border-b border-gray-200 dark:border-gray-700 font-sans text-xs font-medium uppercase tracking-wider text-gray-500 dark:text-gray-400">Row in a vector store</div>
  <table class="w-full">
    <tbody>
      <tr class="border-b border-gray-100 dark:border-gray-800">
        <td class="px-5 py-2.5 font-mono text-xs text-gray-400 dark:text-gray-500 whitespace-nowrap align-top w-28">id</td>
        <td class="px-5 py-2.5 font-mono text-gray-900 dark:text-white">"chunk_042"</td>
      </tr>
      <tr class="border-b border-gray-100 dark:border-gray-800">
        <td class="px-5 py-2.5 font-mono text-xs text-gray-400 dark:text-gray-500 whitespace-nowrap align-top">content</td>
        <td class="px-5 py-2.5 text-gray-700 dark:text-gray-300 font-serif">"To authenticate, include a Bearer token in the Authorization header. Tokens expire after 30 minutes."</td>
      </tr>
      <tr class="border-b border-gray-100 dark:border-gray-800">
        <td class="px-5 py-2.5 font-mono text-xs text-gray-400 dark:text-gray-500 whitespace-nowrap align-top">source</td>
        <td class="px-5 py-2.5 font-mono text-gray-900 dark:text-white">"api-docs/authentication.md"</td>
      </tr>
      <tr>
        <td class="px-5 py-2.5 font-mono text-xs text-gray-400 dark:text-gray-500 whitespace-nowrap align-top">embedding</td>
        <td class="px-5 py-2.5 font-mono text-blue-600 dark:text-blue-400 overflow-x-auto"><span class="text-gray-900 dark:text-white">[0.0231, -0.0892, 0.1345, …]</span> <span class="text-xs text-gray-400 dark:text-gray-500 font-sans">1,536 floats</span></td>
      </tr>
    </tbody>
  </table>
</div>

When a user asks a question, their query gets embedded into the same vector space, and the store finds the rows whose embedding vectors are closest to the query vector.

### How vector search works

Under the hood, a vector store needs to solve a specific problem: given a query vector, find the K most similar vectors out of potentially millions of stored embeddings. A brute-force comparison against every row would be accurate but far too slow at scale.

Most vector stores use **approximate nearest neighbour (ANN)** algorithms to make this tractable. The two most common are:

- **HNSW (Hierarchical Navigable Small World)** — builds a multi-layered graph where each node is a vector connected to its nearest neighbours. Search starts at the top layer (sparse, long-range connections) and drills down to the bottom layer (dense, precise connections). This is what pgvector, Pinecone, and most others use by default. It's fast and accurate, but uses more memory because it stores the full graph in RAM.

- **IVF (Inverted File Index)** — partitions vectors into clusters using k-means, then at query time only searches the clusters closest to the query vector. Faster to build and uses less memory than HNSW, but slightly less accurate because it might miss relevant vectors in neighbouring clusters.

You don't usually need to choose between these directly — your vector store picks a sensible default. But it helps to know the trade-off exists: **speed and memory vs accuracy.** If your retrieval results seem to be missing documents you know should match, check whether the index is configured too aggressively for speed.

### Distance metrics

When the store compares two vectors, it needs a way to measure "closeness." There are three common metrics, each with different properties.

**Cosine similarity** measures the angle between two vectors, ignoring their magnitude. Two vectors pointing in the same direction have a cosine similarity of 1, perpendicular vectors score 0, and opposite vectors score -1.

<div class="not-prose my-6 px-5 py-4 rounded-xl bg-gray-50 dark:bg-gray-800/50 border border-gray-200 dark:border-gray-700 text-center font-mono text-sm text-gray-900 dark:text-white">
  cosine_similarity(<span class="text-blue-600 dark:text-blue-400 font-semibold">A</span>, <span class="text-emerald-600 dark:text-emerald-400 font-semibold">B</span>) = (<span class="text-blue-600 dark:text-blue-400">A</span> · <span class="text-emerald-600 dark:text-emerald-400">B</span>) / (‖<span class="text-blue-600 dark:text-blue-400">A</span>‖ × ‖<span class="text-emerald-600 dark:text-emerald-400">B</span>‖)
</div>

Here's the intuition. Imagine squashing 1,536 dimensions down to just 2:

<div class="not-prose my-8 grid grid-cols-1 sm:grid-cols-2 gap-4">
  <div class="rounded-xl border border-gray-200 dark:border-gray-700 overflow-hidden">
    <div class="px-4 py-2 bg-emerald-50 dark:bg-emerald-900/20 border-b border-gray-200 dark:border-gray-700">
      <span class="text-xs font-semibold uppercase tracking-wider text-emerald-700 dark:text-emerald-400">High similarity — 0.95</span>
    </div>
    <div class="p-4 flex justify-center">
      <svg viewBox="0 0 200 160" class="w-full max-w-[200px]" aria-label="Two vectors pointing in nearly the same direction, showing high cosine similarity">
        <defs>
          <marker id="ah1" markerWidth="8" markerHeight="6" refX="8" refY="3" orient="auto"><polygon points="0 0, 8 3, 0 6" class="fill-blue-500"/></marker>
          <marker id="ah2" markerWidth="8" markerHeight="6" refX="8" refY="3" orient="auto"><polygon points="0 0, 8 3, 0 6" class="fill-emerald-500"/></marker>
        </defs>
        <line x1="30" y1="130" x2="170" y2="130" class="stroke-gray-200 dark:stroke-gray-700" stroke-width="1"/>
        <line x1="30" y1="130" x2="30" y2="20" class="stroke-gray-200 dark:stroke-gray-700" stroke-width="1"/>
        <line x1="30" y1="130" x2="160" y2="40" class="stroke-blue-500" stroke-width="2.5" marker-end="url(#ah1)"/>
        <line x1="30" y1="130" x2="150" y2="30" class="stroke-emerald-500" stroke-width="2.5" marker-end="url(#ah2)"/>
        <path d="M 55 118 A 30 30 0 0 0 52 113" class="stroke-gray-400 dark:stroke-gray-500" stroke-width="1" fill="none"/>
        <text x="64" y="110" class="fill-gray-500 dark:fill-gray-400" font-size="10" font-family="system-ui">12°</text>
        <text x="162" y="38" class="fill-blue-600 dark:fill-blue-400" font-size="9" font-family="system-ui" font-weight="600">A</text>
        <text x="152" y="26" class="fill-emerald-600 dark:fill-emerald-400" font-size="9" font-family="system-ui" font-weight="600">B</text>
      </svg>
    </div>
    <div class="px-4 py-3 bg-gray-50 dark:bg-gray-800/30 border-t border-gray-200 dark:border-gray-700 text-center">
      <p class="text-xs text-gray-600 dark:text-gray-400"><span class="text-blue-600 dark:text-blue-400 font-semibold">A</span> "reset my password"</p>
      <p class="text-xs text-gray-600 dark:text-gray-400"><span class="text-emerald-600 dark:text-emerald-400 font-semibold">B</span> "change my login credentials"</p>
    </div>
  </div>
  <div class="rounded-xl border border-gray-200 dark:border-gray-700 overflow-hidden">
    <div class="px-4 py-2 bg-red-50 dark:bg-red-900/20 border-b border-gray-200 dark:border-gray-700">
      <span class="text-xs font-semibold uppercase tracking-wider text-red-700 dark:text-red-400">Low similarity — 0.15</span>
    </div>
    <div class="p-4 flex justify-center">
      <svg viewBox="0 0 200 160" class="w-full max-w-[200px]" aria-label="Two vectors pointing in very different directions, showing low cosine similarity">
        <defs>
          <marker id="ah3" markerWidth="8" markerHeight="6" refX="8" refY="3" orient="auto"><polygon points="0 0, 8 3, 0 6" class="fill-blue-500"/></marker>
          <marker id="ah4" markerWidth="8" markerHeight="6" refX="8" refY="3" orient="auto"><polygon points="0 0, 8 3, 0 6" class="fill-red-500"/></marker>
        </defs>
        <line x1="30" y1="130" x2="170" y2="130" class="stroke-gray-200 dark:stroke-gray-700" stroke-width="1"/>
        <line x1="30" y1="130" x2="30" y2="20" class="stroke-gray-200 dark:stroke-gray-700" stroke-width="1"/>
        <line x1="30" y1="130" x2="150" y2="30" class="stroke-blue-500" stroke-width="2.5" marker-end="url(#ah3)"/>
        <line x1="30" y1="130" x2="170" y2="112" class="stroke-red-500" stroke-width="2.5" marker-end="url(#ah4)"/>
        <path d="M 58 120 A 30 30 0 0 0 52 113" class="stroke-gray-400 dark:stroke-gray-500" stroke-width="1" fill="none"/>
        <text x="62" y="115" class="fill-gray-500 dark:fill-gray-400" font-size="10" font-family="system-ui">80°</text>
        <text x="152" y="26" class="fill-blue-600 dark:fill-blue-400" font-size="9" font-family="system-ui" font-weight="600">A</text>
        <text x="170" y="108" class="fill-red-600 dark:fill-red-400" font-size="9" font-family="system-ui" font-weight="600">B</text>
      </svg>
    </div>
    <div class="px-4 py-3 bg-gray-50 dark:bg-gray-800/30 border-t border-gray-200 dark:border-gray-700 text-center">
      <p class="text-xs text-gray-600 dark:text-gray-400"><span class="text-blue-600 dark:text-blue-400 font-semibold">A</span> "reset my password"</p>
      <p class="text-xs text-gray-600 dark:text-gray-400"><span class="text-red-600 dark:text-red-400 font-semibold">B</span> "quarterly revenue report"</p>
    </div>
  </div>
</div>

Cosine similarity is the default for most embedding models because it normalises for vector length — a short chunk and a long chunk about the same topic will still score high.

**Dot product** is the numerator of cosine similarity without the normalisation:

<div class="not-prose my-6 rounded-xl border border-gray-200 dark:border-gray-700 overflow-hidden text-sm">
  <div class="px-5 py-3 bg-gray-50 dark:bg-gray-800/50 border-b border-gray-200 dark:border-gray-700 text-center font-mono text-gray-900 dark:text-white">
    dot_product(<span class="text-blue-600 dark:text-blue-400 font-semibold">A</span>, <span class="text-emerald-600 dark:text-emerald-400 font-semibold">B</span>) = <span class="text-blue-600 dark:text-blue-400">A</span> · <span class="text-emerald-600 dark:text-emerald-400">B</span> = Σ(aᵢ × bᵢ)
  </div>
  <div class="px-5 py-4 space-y-2 font-mono text-gray-700 dark:text-gray-300">
    <p><span class="text-blue-600 dark:text-blue-400 font-semibold">A</span> = [0.5, 0.3] &nbsp;&nbsp; <span class="text-emerald-600 dark:text-emerald-400 font-semibold">B</span> = [0.4, 0.6]</p>
    <p class="text-gray-400 dark:text-gray-500">dot = (<span class="text-gray-900 dark:text-white">0.5 × 0.4</span>) + (<span class="text-gray-900 dark:text-white">0.3 × 0.6</span>) = 0.20 + 0.18 = <span class="text-gray-900 dark:text-white font-semibold">0.38</span></p>
  </div>
</div>

It's faster to compute because it skips the magnitude calculation. But it's sensitive to vector length — a longer vector will score higher just because it's longer, not because it's more similar. This works fine when your embedding model produces normalised vectors (many do), but can give misleading results if they're not normalised.

**Euclidean distance** measures the straight-line distance between two points in the vector space:

<div class="not-prose my-6 rounded-xl border border-gray-200 dark:border-gray-700 overflow-hidden text-sm">
  <div class="px-5 py-3 bg-gray-50 dark:bg-gray-800/50 border-b border-gray-200 dark:border-gray-700 text-center font-mono text-gray-900 dark:text-white">
    euclidean(<span class="text-blue-600 dark:text-blue-400 font-semibold">A</span>, <span class="text-emerald-600 dark:text-emerald-400 font-semibold">B</span>) = √(Σ(aᵢ - bᵢ)²)
  </div>
  <div class="flex flex-col sm:flex-row items-center gap-4 p-5">
    <div class="flex justify-center">
      <svg viewBox="0 0 180 160" class="w-full max-w-[180px]" aria-label="Two points A and B in 2D space with a dashed line showing the Euclidean distance between them">
        <line x1="30" y1="140" x2="160" y2="140" class="stroke-gray-200 dark:stroke-gray-700" stroke-width="1"/>
        <line x1="30" y1="140" x2="30" y2="20" class="stroke-gray-200 dark:stroke-gray-700" stroke-width="1"/>
        <line x1="110" y1="110" x2="80" y2="40" class="stroke-amber-500" stroke-width="2" stroke-dasharray="6 4"/>
        <line x1="110" y1="110" x2="80" y2="110" class="stroke-gray-300 dark:stroke-gray-600" stroke-width="1" stroke-dasharray="3 3"/>
        <line x1="80" y1="110" x2="80" y2="40" class="stroke-gray-300 dark:stroke-gray-600" stroke-width="1" stroke-dasharray="3 3"/>
        <circle cx="110" cy="110" r="5" class="fill-blue-500"/>
        <circle cx="80" cy="40" r="5" class="fill-emerald-500"/>
        <text x="118" y="116" class="fill-blue-600 dark:fill-blue-400" font-size="11" font-family="system-ui" font-weight="600">A</text>
        <text x="86" y="38" class="fill-emerald-600 dark:fill-emerald-400" font-size="11" font-family="system-ui" font-weight="600">B</text>
        <text x="100" y="70" class="fill-amber-600 dark:fill-amber-400" font-size="10" font-family="system-ui" font-weight="500" transform="rotate(-67, 100, 70)">d</text>
        <text x="85" y="126" class="fill-gray-400 dark:fill-gray-500" font-size="9" font-family="system-ui">Δx</text>
        <text x="62" y="78" class="fill-gray-400 dark:fill-gray-500" font-size="9" font-family="system-ui">Δy</text>
      </svg>
    </div>
    <div class="font-mono text-gray-700 dark:text-gray-300 space-y-1.5">
      <p><span class="text-blue-600 dark:text-blue-400 font-semibold">A</span> = (0.5, 0.3) &nbsp;&nbsp; <span class="text-emerald-600 dark:text-emerald-400 font-semibold">B</span> = (0.4, 0.6)</p>
      <p class="text-gray-400 dark:text-gray-500"><span class="text-amber-600 dark:text-amber-400">d</span> = √(0.1² + 0.3²) = √0.10 ≈ <span class="text-gray-900 dark:text-white font-semibold">0.316</span></p>
    </div>
  </div>
</div>

Lower distance means more similar. It works well in many cases but is sensitive to magnitude — two vectors about the same topic but with different scales will appear further apart than they should.

> **Which one to use?** Start with **cosine similarity.** It's the most forgiving and the default for most embedding models. Only switch to dot product if you've confirmed your embeddings are normalised and you need the speed. Use Euclidean distance only if your specific model recommends it.

In practice, most retrieval results with a cosine similarity above **0.8** are strong matches. Between **0.5 and 0.8** is "maybe relevant." Below **0.5** is usually noise. These thresholds vary by model and domain, but they're a reasonable starting heuristic.

### Multi-tenancy

If your application serves multiple users or organisations, you need to make sure that user A's documents never appear in user B's search results. This is **multi-tenancy** for vector stores, and there are a few approaches depending on your scale.

**Metadata filtering** is the simplest. Store a `tenant_id` field alongside each embedding and filter on it at query time:

```typescript
// At storage time — tag every chunk with its owner
await pool.query(
  `INSERT INTO documents (content, tenant_id, embedding)
   VALUES ($1, $2, $3)`,
  [chunk.text, tenantId, JSON.stringify(embedding)]
);

// At query time — only search this tenant's documents
const result = await pool.query(
  `SELECT content, 1 - (embedding <=> $1) AS similarity
   FROM documents
   WHERE tenant_id = $2
   ORDER BY embedding <=> $1
   LIMIT $3`,
  [JSON.stringify(queryEmbedding), tenantId, topK]
);
```

This works well for small to medium scale. The vector search happens across all rows and the `WHERE` clause filters the results. With pgvector you can add a composite index on `(tenant_id, embedding)` to make this efficient.

**Namespace separation** is offered by managed services like Pinecone. Each tenant gets their own namespace within the same index — the search is scoped to that namespace from the start, so there's no risk of cross-tenant leakage and the search is faster because it only scans one partition.

**Separate indexes/tables** is the most isolated approach — one vector index per tenant. This gives you hard data separation and independent scaling, but adds operational complexity. It makes sense when you have a small number of large tenants (think enterprise customers with millions of documents each) rather than thousands of small ones.

For most applications, **metadata filtering with a tenant ID** is the right starting point. It's simple, it works with any vector store, and you can upgrade to namespace separation later if query performance becomes an issue.

### Choosing a vector store

For the store itself, the options range from lightweight to fully managed. **pgvector** extends PostgreSQL with vector search — if you're already running Postgres, this is the lowest-friction option. **ChromaDB** is good for local development and prototyping. **Pinecone** is a managed service that handles scaling for you. For small datasets, even **SQLite with extensions** can work.

My advice: **don't overthink the vector store choice early on.** The retrieval quality comes from your chunking and embedding strategy, not from which database you use. Start with whatever's easiest to integrate.

Here's a minimal example storing and querying embeddings with pgvector:

```typescript
import { Pool } from "pg";
import { embed } from "./embeddings";

const pool = new Pool({ connectionString: process.env.DATABASE_URL });

async function storeChunks(chunks: { text: string; source: string }[]) {
  for (const chunk of chunks) {
    const embedding = await embed(chunk.text);
    await pool.query(
      `INSERT INTO documents (content, source, embedding)
       VALUES ($1, $2, $3)`,
      [chunk.text, chunk.source, JSON.stringify(embedding)]
    );
  }
}

async function search(query: string, topK = 5) {
  const queryEmbedding = await embed(query);
  const result = await pool.query(
    `SELECT content, source, 1 - (embedding <=> $1) AS similarity
     FROM documents
     ORDER BY embedding <=> $1
     LIMIT $2`,
    [JSON.stringify(queryEmbedding), topK]
  );
  return result.rows;
}
```

The `<=>` operator is pgvector's cosine distance. Lower distance means higher similarity — that's why we sort ascending and compute `1 - distance` to get a similarity score.

---

## The Retrieval Pipeline

With embeddings, chunks, and a vector store in place, the retrieval pipeline is the thing that ties it all together. The flow is: **query → embed → search → rank → inject into prompt**.

```python
import openai

def retrieve_and_generate(query: str, top_k: int = 5) -> str:
    # 1. Embed the user's query
    query_embedding = openai.embeddings.create(
        model="text-embedding-3-small",
        input=query,
    ).data[0].embedding

    # 2. Search the vector store
    results = vector_store.search(query_embedding, top_k=top_k)

    # 3. Build context from retrieved chunks
    context = "\n\n---\n\n".join(
        f"[Source: {r['source']}]\n{r['content']}"
        for r in results
    )

    # 4. Generate a grounded response
    response = openai.chat.completions.create(
        model="gpt-4o",
        messages=[
            {
                "role": "system",
                "content": (
                    "Answer the user's question using only the provided context. "
                    "If the context doesn't contain enough information, say so. "
                    "Cite your sources."
                ),
            },
            {
                "role": "user",
                "content": f"Context:\n{context}\n\nQuestion: {query}",
            },
        ],
    )

    return response.choices[0].message.content
```

A few things worth noting. The system prompt is doing important work — it tells the model to use only the provided context and to admit when it doesn't have enough information. Without these instructions, the model will happily hallucinate an answer that sounds plausible but isn't grounded in your data. The source citations in the context block give the model something to reference when it answers.

This is the basic pipeline. In production you'd add **metadata filtering** (only search documents from a specific category or date range), **re-ranking** (use a cross-encoder to re-score the top results for better precision), and **hybrid search** (combine vector similarity with keyword matching for cases where exact terms matter).

---

## What Actually Matters

Here's where the leverage actually is:

**Chunk size tuning matters more than vector database choice.** I've seen retrieval quality swing dramatically just by changing chunk size from 1,000 tokens to 500. The "right" size depends on your content — short factual documents want smaller chunks, longer narrative documents want larger ones. Test this empirically, not theoretically.

**Metadata filtering is underrated.** Being able to filter by document type, date, or category before the vector search runs is extremely powerful. It reduces the search space and improves relevance significantly. Build this in from the start.

**Re-ranking improves precision.** Vector search gives you recall — it finds documents that are roughly relevant. A cross-encoder re-ranker then scores each result against the actual query with much higher fidelity. It's an extra API call, but the precision improvement is worth it for most use cases.

**Start simple.** Don't build a complex multi-stage retrieval system on day one. Start with basic chunking, a single embedding model, and cosine similarity search. Measure what's working and what isn't. Add complexity — re-ranking, hybrid search, query expansion — only when you've identified a specific retrieval failure that the simple approach can't handle.

The biggest mistake I see is over-engineering the retrieval pipeline before understanding whether the problem is actually retrieval or something else entirely — like bad chunking, missing documents, or a poorly written system prompt.

---

## Questions?

If you're building a RAG system and want to talk through your approach — chunking strategy, model choices, evaluation, or anything else — feel free to reach out. I'm happy to help where I can.

You can find me at [rmccaffr@tcd.ie](mailto:rmccaffr@tcd.ie) or on [LinkedIn](https://linkedin.com/in/robbiemc8).

---

*This is part 1 of [AI Agent Architecture](/blog) — a practitioner's guide to building with LLMs. Next up: evaluating RAG systems.*
