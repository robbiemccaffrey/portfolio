---
title: "How LLMs Are Created: From Raw Text to Reasoning"
date: 2026-03-15
description: "A walkthrough of the four stages that turn raw internet text into an LLM that can reason — pretraining, supervised fine-tuning, RLHF, and the new RLVR paradigm."
tags: ["ai", "llm", "deep-learning", "rlvr"]
draft: true
---

## How a Language Model Gets Built

Most people now interact with ChatGPT, Claude, or Gemini daily — asking questions, writing code, summarising documents. But very few understand what actually goes into building one. The gap between "I use it" and "I understand it" is enormous, and closing that gap changes how you think about what these systems can and can't do.

The single best resource I've found for understanding this is Andrej Karpathy's [Deep Dive into LLMs like ChatGPT](https://www.youtube.com/watch?v=7xTGNNLPyMI). This post distils the key ideas from that talk and extends them with what changed in 2025 — specifically, the emergence of RLVR as a fourth training stage that unlocked genuine reasoning capabilities.

---

## Stage 1: Pretraining — Reading the Internet

Everything starts with data. Pretraining is the foundational stage where a model ingests an enormous corpus of text — web pages, books, Wikipedia, code repositories, forums, academic papers — and learns statistical patterns across all of it.

The scale is staggering. Datasets like [FineWeb](https://huggingface.co/datasets/HuggingFaceFW/fineweb) contain over 1.2 billion web pages, heavily filtered for quality. Raw web crawls are noisy — full of spam, duplicates, and low-quality content — so significant effort goes into cleaning and deduplication before a single token is processed.

### Data Curation — The Hidden Lever

It's worth noting how much smarter data curation has become. When GPT-2 was trained in 2019, the approach to data was relatively crude — scrape a lot of the web and do basic filtering. ChatGPT launched in late 2022 on a much larger but still imperfect dataset. Since then, the industry has learned that **data quality matters as much as data quantity**. Modern pretraining pipelines use aggressive deduplication (near-duplicate detection at scale), quality classifiers trained to score every page, toxicity and PII filters, domain-specific heuristics (is this machine-generated spam?), and careful balancing across domains — more code, more scientific text, less SEO content. FineWeb, for example, starts from 96 billion web pages and filters down to roughly 15 billion high-quality pages. The same model architecture trained on better-curated data produces a meaningfully smarter base model. Data curation has become one of the most important — and least visible — competitive advantages in AI.

### Neural Networks — A Quick Primer

Before diving into tokenization and transformers, it's worth briefly covering what a neural network actually is — since everything that follows is built on top of one.

A neural network is a mathematical function loosely inspired by the brain. It's made of layers of **neurons** — each neuron takes some numbers as input, multiplies them by **weights** (learnable parameters), adds them up, and passes the result through an activation function. Stack millions of these neurons in layers, and you get a system that can learn complex patterns from data.

<div class="not-prose my-8">
  <div class="rounded-xl border border-gray-200 dark:border-gray-700 overflow-hidden">
    <div class="px-5 py-2.5 bg-gray-50 dark:bg-gray-800/50 border-b border-gray-200 dark:border-gray-700 font-sans text-xs font-medium uppercase tracking-wider text-gray-500 dark:text-gray-400">Neural network — simplified</div>
    <div class="px-4 py-6 sm:px-8 sm:py-8">
      <svg viewBox="0 0 700 200" class="w-full" aria-label="Simplified diagram of a neural network with an input layer, two hidden layers, and an output layer. Neurons are connected by weighted edges.">
        {/* Input layer */}
        <text x="80" y="15" text-anchor="middle" font-size="10" font-family="system-ui" font-weight="600" class="fill-blue-600 dark:fill-blue-400">Input</text>
        {[50, 90, 130].map((y, i) => (
          <circle key={`i-${i}`} cx="80" cy={y + 25} r="14" class="fill-blue-100 dark:fill-blue-900/40 stroke-blue-400 dark:stroke-blue-600" stroke-width="1.5"/>
        ))}

        {/* Hidden layer 1 */}
        <text x="270" y="15" text-anchor="middle" font-size="10" font-family="system-ui" font-weight="600" class="fill-purple-600 dark:fill-purple-400">Hidden layer 1</text>
        {[40, 80, 120, 160].map((y, i) => (
          <circle key={`h1-${i}`} cx="270" cy={y + 20} r="14" class="fill-purple-100 dark:fill-purple-900/40 stroke-purple-400 dark:stroke-purple-600" stroke-width="1.5"/>
        ))}

        {/* Hidden layer 2 */}
        <text x="460" y="15" text-anchor="middle" font-size="10" font-family="system-ui" font-weight="600" class="fill-purple-600 dark:fill-purple-400">Hidden layer 2</text>
        {[40, 80, 120, 160].map((y, i) => (
          <circle key={`h2-${i}`} cx="460" cy={y + 20} r="14" class="fill-purple-100 dark:fill-purple-900/40 stroke-purple-400 dark:stroke-purple-600" stroke-width="1.5"/>
        ))}

        {/* Output layer */}
        <text x="620" y="15" text-anchor="middle" font-size="10" font-family="system-ui" font-weight="600" class="fill-emerald-600 dark:fill-emerald-400">Output</text>
        {[70, 120].map((y, i) => (
          <circle key={`o-${i}`} cx="620" cy={y + 15} r="14" class="fill-emerald-100 dark:fill-emerald-900/40 stroke-emerald-400 dark:stroke-emerald-600" stroke-width="1.5"/>
        ))}

        {/* Connections input → hidden 1 */}
        {[75, 115, 155].map((iy) =>
          [60, 100, 140, 180].map((hy, j) => (
            <line key={`ih-${iy}-${j}`} x1="94" y1={iy} x2="256" y2={hy} class="stroke-gray-200 dark:stroke-gray-700" stroke-width="0.5"/>
          ))
        )}

        {/* Connections hidden 1 → hidden 2 */}
        {[60, 100, 140, 180].map((hy) =>
          [60, 100, 140, 180].map((h2y, j) => (
            <line key={`hh-${hy}-${j}`} x1="284" y1={hy} x2="446" y2={h2y} class="stroke-gray-200 dark:stroke-gray-700" stroke-width="0.5"/>
          ))
        )}

        {/* Connections hidden 2 → output */}
        {[60, 100, 140, 180].map((hy) =>
          [85, 135].map((oy, j) => (
            <line key={`ho-${hy}-${j}`} x1="474" y1={hy} x2="606" y2={oy} class="stroke-gray-200 dark:stroke-gray-700" stroke-width="0.5"/>
          ))
        )}
      </svg>
    </div>
    <div class="px-5 py-3 bg-gray-50 dark:bg-gray-800/50 border-t border-gray-200 dark:border-gray-700 text-xs text-gray-500 dark:text-gray-400">
      Each line is a <strong>weight</strong> — a number the network adjusts during training. GPT-4 has hundreds of billions of these. "Training" means finding the weight values that make the network's predictions match reality.
    </div>
  </div>
</div>

Training works by showing the network examples, measuring how wrong its predictions are (the **loss**), and nudging all the weights slightly in the direction that reduces the error. Do this billions of times across trillions of examples, and the weights converge on values that make the network remarkably good at its task. The key insight: nobody programs the rules. The network discovers them from the data.

A **transformer** is a specific type of neural network architecture, introduced in the 2017 paper ["Attention Is All You Need"](https://arxiv.org/abs/1706.03762). What makes transformers special is the **attention mechanism** — the ability to look at all previous tokens simultaneously and decide which ones are relevant for predicting the next one. Previous architectures (RNNs, LSTMs) processed tokens sequentially, like reading one word at a time and trying to remember. Transformers can look at the whole sentence at once. This is what made them scale — and it's why every modern LLM is built on them.

Before any of this learning can happen, text needs to be converted into something a neural network can process. Raw text is a one-dimensional sequence of characters — just a long string. **Tokenization** converts this string into a sequence of symbols from a fixed vocabulary using Byte Pair Encoding (BPE). The algorithm starts with individual characters, then repeatedly merges the most frequent adjacent pairs into new tokens. After enough merges, you get a vocabulary — GPT-4's is roughly 100,000 tokens.

Here's how BPE builds up its vocabulary. It starts with single characters and iteratively merges the most common pairs:

<div class="not-prose my-8">
  <div class="rounded-xl border border-gray-200 dark:border-gray-700 overflow-hidden">
    <div class="px-5 py-2.5 bg-gray-50 dark:bg-gray-800/50 border-b border-gray-200 dark:border-gray-700 font-sans text-xs font-medium uppercase tracking-wider text-gray-500 dark:text-gray-400">Byte Pair Encoding — how tokens are built</div>
    <div class="px-4 py-6 sm:px-8 sm:py-8">
      <svg viewBox="0 0 700 280" class="w-full" aria-label="Diagram showing how Byte Pair Encoding iteratively merges character pairs into tokens">
        {/* Row 1: Raw characters */}
        <text x="16" y="28" font-size="11" font-family="system-ui" font-weight="600" class="fill-gray-500 dark:fill-gray-400">Start</text>
        {["l","o","w","e","r","_","l","o","w","_","n","e","w","e","r"].map((ch, i) => (
          <g key={`r1-${i}`}>
            <rect x={80 + i * 40} y="10" width="34" height="32" rx="6" class="fill-gray-100 dark:fill-gray-800 stroke-gray-300 dark:stroke-gray-600" stroke-width="1"/>
            <text x={80 + i * 40 + 17} y="31" text-anchor="middle" font-size="13" font-family="ui-monospace, monospace" class="fill-gray-800 dark:fill-gray-200">{ch === "_" ? "·" : ch}</text>
          </g>
        ))}
        <text x="690" y="31" text-anchor="end" font-size="10" font-family="system-ui" class="fill-gray-400 dark:fill-gray-500">15 tokens</text>

        {/* Arrow 1 */}
        <line x1="350" y1="50" x2="350" y2="72" class="stroke-gray-300 dark:stroke-gray-600" stroke-width="1.5" marker-end="url(#arrow)"/>
        <text x="370" y="66" font-size="10" font-family="system-ui" class="fill-blue-600 dark:fill-blue-400">merge "l" + "o" → "lo"</text>

        {/* Row 2: After first merge */}
        <text x="16" y="98" font-size="11" font-family="system-ui" font-weight="600" class="fill-gray-500 dark:fill-gray-400">Pass 1</text>
        {["lo","w","e","r","_","lo","w","_","n","e","w","e","r"].map((ch, i) => (
          <g key={`r2-${i}`}>
            <rect x={80 + i * 44} y="80" width={ch.length > 1 ? 40 : 34} height="32" rx="6" class={ch.length > 1 ? "fill-blue-50 dark:fill-blue-900/30 stroke-blue-300 dark:stroke-blue-700" : "fill-gray-100 dark:fill-gray-800 stroke-gray-300 dark:stroke-gray-600"} stroke-width="1"/>
            <text x={80 + i * 44 + (ch.length > 1 ? 20 : 17)} y="101" text-anchor="middle" font-size="13" font-family="ui-monospace, monospace" class={ch.length > 1 ? "fill-blue-700 dark:fill-blue-300" : "fill-gray-800 dark:fill-gray-200"}>{ch === "_" ? "·" : ch}</text>
          </g>
        ))}
        <text x="690" y="101" text-anchor="end" font-size="10" font-family="system-ui" class="fill-gray-400 dark:fill-gray-500">13 tokens</text>

        {/* Arrow 2 */}
        <line x1="350" y1="120" x2="350" y2="142" class="stroke-gray-300 dark:stroke-gray-600" stroke-width="1.5" marker-end="url(#arrow)"/>
        <text x="370" y="136" font-size="10" font-family="system-ui" class="fill-purple-600 dark:fill-purple-400">merge "lo" + "w" → "low"</text>

        {/* Row 3: After second merge */}
        <text x="16" y="168" font-size="11" font-family="system-ui" font-weight="600" class="fill-gray-500 dark:fill-gray-400">Pass 2</text>
        {["low","e","r","_","low","_","n","e","w","e","r"].map((ch, i) => (
          <g key={`r3-${i}`}>
            <rect x={80 + i * 50} y="150" width={ch.length > 1 ? 44 : 34} height="32" rx="6" class={ch.length > 1 ? "fill-purple-50 dark:fill-purple-900/30 stroke-purple-300 dark:stroke-purple-700" : "fill-gray-100 dark:fill-gray-800 stroke-gray-300 dark:stroke-gray-600"} stroke-width="1"/>
            <text x={80 + i * 50 + (ch.length > 1 ? 22 : 17)} y="171" text-anchor="middle" font-size="13" font-family="ui-monospace, monospace" class={ch.length > 1 ? "fill-purple-700 dark:fill-purple-300" : "fill-gray-800 dark:fill-gray-200"}>{ch === "_" ? "·" : ch}</text>
          </g>
        ))}
        <text x="690" y="171" text-anchor="end" font-size="10" font-family="system-ui" class="fill-gray-400 dark:fill-gray-500">11 tokens</text>

        {/* Arrow 3 */}
        <line x1="350" y1="190" x2="350" y2="212" class="stroke-gray-300 dark:stroke-gray-600" stroke-width="1.5" marker-end="url(#arrow)"/>
        <text x="370" y="206" font-size="10" font-family="system-ui" class="fill-gray-500 dark:fill-gray-400">…repeated ~100,000 times</text>

        {/* Row 4: Final vocabulary tokens */}
        <text x="16" y="240" font-size="11" font-family="system-ui" font-weight="600" class="fill-gray-500 dark:fill-gray-400">Final</text>
        {["lower","_","low","_","newer"].map((ch, i) => (
          <g key={`r4-${i}`}>
            <rect x={80 + i * 100} y="222" width={ch === "_" ? 34 : ch.length * 12 + 20} height="32" rx="6" class={ch === "_" ? "fill-gray-100 dark:fill-gray-800 stroke-gray-300 dark:stroke-gray-600" : "fill-emerald-50 dark:fill-emerald-900/30 stroke-emerald-300 dark:stroke-emerald-700"} stroke-width="1"/>
            <text x={80 + i * 100 + (ch === "_" ? 17 : (ch.length * 12 + 20) / 2)} y="243" text-anchor="middle" font-size="13" font-family="ui-monospace, monospace" class={ch === "_" ? "fill-gray-800 dark:fill-gray-200" : "fill-emerald-700 dark:fill-emerald-300"}>{ch === "_" ? "·" : ch}</text>
          </g>
        ))}
        <text x="690" y="243" text-anchor="end" font-size="10" font-family="system-ui" class="fill-gray-400 dark:fill-gray-500">5 tokens</text>

        {/* Arrow marker */}
        <defs>
          <marker id="arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
            <path d="M 0 0 L 10 5 L 0 10 z" class="fill-gray-400 dark:fill-gray-500"/>
          </marker>
        </defs>
      </svg>
    </div>
  </div>
</div>

Common words like "the" end up as single tokens, ordinary words like "understanding" might be one or two tokens, and rare words get split into several pieces. The key insight is that the model never sees raw text — it sees a stream of integer IDs, one dimension, one token at a time.

Now look at what tokenization actually does to a real sentence. Each coloured block is a single token — one symbol in the model's vocabulary:

<div class="not-prose my-8">
  <div class="rounded-xl border border-gray-200 dark:border-gray-700 overflow-hidden">
    <div class="px-5 py-2.5 bg-gray-50 dark:bg-gray-800/50 border-b border-gray-200 dark:border-gray-700 font-sans text-xs font-medium uppercase tracking-wider text-gray-500 dark:text-gray-400">Tokenization in practice</div>
    <div class="px-4 py-6 sm:px-8 sm:py-8 space-y-6">
      {/* Input text */}
      <div>
        <div class="text-xs font-semibold uppercase tracking-wider text-gray-500 dark:text-gray-400 mb-2">Raw text</div>
        <p class="text-base font-mono text-gray-800 dark:text-gray-200">"The cat sat on the mat"</p>
      </div>

      <div class="flex items-center justify-center">
        <span class="text-gray-400 dark:text-gray-500 text-lg">↓</span>
        <span class="text-xs font-sans text-gray-400 dark:text-gray-500 ml-2">tokenizer</span>
      </div>

      {/* Tokenized output */}
      <div>
        <div class="text-xs font-semibold uppercase tracking-wider text-gray-500 dark:text-gray-400 mb-3">Tokens — each block = 1 token</div>
        <div class="flex flex-wrap gap-1.5">
          <span class="px-2.5 py-1.5 rounded-md bg-blue-100 dark:bg-blue-900/40 text-blue-800 dark:text-blue-200 font-mono text-sm">The</span>
          <span class="px-2.5 py-1.5 rounded-md bg-emerald-100 dark:bg-emerald-900/40 text-emerald-800 dark:text-emerald-200 font-mono text-sm">·cat</span>
          <span class="px-2.5 py-1.5 rounded-md bg-purple-100 dark:bg-purple-900/40 text-purple-800 dark:text-purple-200 font-mono text-sm">·sat</span>
          <span class="px-2.5 py-1.5 rounded-md bg-orange-100 dark:bg-orange-900/40 text-orange-800 dark:text-orange-200 font-mono text-sm">·on</span>
          <span class="px-2.5 py-1.5 rounded-md bg-pink-100 dark:bg-pink-900/40 text-pink-800 dark:text-pink-200 font-mono text-sm">·the</span>
          <span class="px-2.5 py-1.5 rounded-md bg-yellow-100 dark:bg-yellow-900/40 text-yellow-800 dark:text-yellow-200 font-mono text-sm">·mat</span>
        </div>
      </div>

      <div class="flex items-center justify-center">
        <span class="text-gray-400 dark:text-gray-500 text-lg">↓</span>
        <span class="text-xs font-sans text-gray-400 dark:text-gray-500 ml-2">vocabulary lookup</span>
      </div>

      {/* Integer IDs */}
      <div>
        <div class="text-xs font-semibold uppercase tracking-wider text-gray-500 dark:text-gray-400 mb-3">Token IDs — what the model actually sees</div>
        <div class="flex flex-wrap gap-1.5">
          <span class="px-2.5 py-1.5 rounded-md bg-blue-100 dark:bg-blue-900/40 text-blue-800 dark:text-blue-200 font-mono text-sm font-bold">791</span>
          <span class="px-2.5 py-1.5 rounded-md bg-emerald-100 dark:bg-emerald-900/40 text-emerald-800 dark:text-emerald-200 font-mono text-sm font-bold">4,879</span>
          <span class="px-2.5 py-1.5 rounded-md bg-purple-100 dark:bg-purple-900/40 text-purple-800 dark:text-purple-200 font-mono text-sm font-bold">9,107</span>
          <span class="px-2.5 py-1.5 rounded-md bg-orange-100 dark:bg-orange-900/40 text-orange-800 dark:text-orange-200 font-mono text-sm font-bold">389</span>
          <span class="px-2.5 py-1.5 rounded-md bg-pink-100 dark:bg-pink-900/40 text-pink-800 dark:text-pink-200 font-mono text-sm font-bold">290</span>
          <span class="px-2.5 py-1.5 rounded-md bg-yellow-100 dark:bg-yellow-900/40 text-yellow-800 dark:text-yellow-200 font-mono text-sm font-bold">7,108</span>
        </div>
        <p class="text-xs text-gray-500 dark:text-gray-400 mt-3">The · represents a space. "The" and "·the" are different tokens — capitalisation matters. The model's entire input is this sequence of integers.</p>
      </div>
    </div>
  </div>
</div>

You can explore this yourself with [Tiktokenizer](https://tiktokenizer.vercel.app/) — paste some text and watch how it splits. Try your name, some code, text in another language. You'll notice the tokenizer is opinionated: English prose is efficient (few tokens per word), while unusual strings burn through tokens fast.

### The Transformer — A DJ's Switchboard for Meaning

The transformer architecture — billions of parameters arranged in **attention layers** — then learns a single objective: **predict the next token**. But how does it decide which tokens matter for the prediction?

Think of it like a DJ's mixing board. A DJ has dozens of channels — vocals, bass, drums, synths — and for every moment in a track, they're adjusting which channels to boost and which to suppress. The transformer's attention mechanism works the same way. For every token the model processes, it has to decide: which of the previous tokens should I pay attention to right now? When predicting the word after "The capital of France is", the attention mechanism learns to boost the signal from "France" and "capital" while suppressing "The" and "of" — just like a DJ pulling up the vocal channel and dropping the hi-hat for a breakdown.

Each attention layer is like a different DJ working the same track simultaneously — one might focus on grammatical structure, another on semantic meaning, another on positional patterns. Stack dozens of these layers and you get a system that can attend to complex, long-range relationships in text.

If you want to see this in action, Brendan Bycroft built an incredible [interactive transformer visualization](https://bbycroft.net/llm) that lets you watch tokens flow through each layer of a working model. You can see attention patterns forming in real time — which tokens are "looking at" which other tokens at each stage. It makes the abstract architecture tangible.

Given the sequence of tokens so far, the model outputs a probability distribution over the entire vocabulary — a percentage chance for each of the ~100,000 possible next tokens.

<div class="not-prose my-8">
  <div class="rounded-xl border border-gray-200 dark:border-gray-700 overflow-hidden">
    <div class="px-5 py-2.5 bg-gray-50 dark:bg-gray-800/50 border-b border-gray-200 dark:border-gray-700 font-sans text-xs font-medium uppercase tracking-wider text-gray-500 dark:text-gray-400">Next-token prediction — probability distribution</div>
    <div class="px-4 py-6 sm:px-8 sm:py-8">
      <div class="mb-4">
        <span class="text-xs font-semibold uppercase tracking-wider text-gray-500 dark:text-gray-400">Input:</span>
        <span class="font-mono text-sm text-gray-800 dark:text-gray-200 ml-2">"The capital of France is"</span>
        <span class="text-gray-400 dark:text-gray-500 ml-1">→ what comes next?</span>
      </div>

      <svg viewBox="0 0 700 200" class="w-full" aria-label="Bar chart showing probability distribution over next tokens. Paris has the highest probability at 62%, followed by the at 8%, a at 5%, located at 4%, and other tokens sharing the remaining probability.">
        {/* Bars */}
        <g>
          {/* Paris */}
          <rect x="60" y="10" width="310" height="28" rx="4" class="fill-emerald-400 dark:fill-emerald-500"/>
          <text x="52" y="29" text-anchor="end" font-size="12" font-family="ui-monospace, monospace" font-weight="600" class="fill-emerald-700 dark:fill-emerald-300">Paris</text>
          <text x="380" y="29" font-size="11" font-family="system-ui" font-weight="600" class="fill-emerald-700 dark:fill-emerald-300">62%</text>

          {/* the */}
          <rect x="60" y="46" width="40" height="28" rx="4" class="fill-blue-300 dark:fill-blue-500/70"/>
          <text x="52" y="65" text-anchor="end" font-size="12" font-family="ui-monospace, monospace" class="fill-blue-700 dark:fill-blue-300">the</text>
          <text x="108" y="65" font-size="11" font-family="system-ui" class="fill-blue-600 dark:fill-blue-400">8%</text>

          {/* a */}
          <rect x="60" y="82" width="25" height="28" rx="4" class="fill-blue-200 dark:fill-blue-600/50"/>
          <text x="52" y="101" text-anchor="end" font-size="12" font-family="ui-monospace, monospace" class="fill-blue-700 dark:fill-blue-300">a</text>
          <text x="93" y="101" font-size="11" font-family="system-ui" class="fill-blue-600 dark:fill-blue-400">5%</text>

          {/* located */}
          <rect x="60" y="118" width="20" height="28" rx="4" class="fill-blue-100 dark:fill-blue-700/40"/>
          <text x="52" y="137" text-anchor="end" font-size="12" font-family="ui-monospace, monospace" class="fill-blue-700 dark:fill-blue-300">located</text>
          <text x="88" y="137" font-size="11" font-family="system-ui" class="fill-blue-600 dark:fill-blue-400">4%</text>

          {/* known */}
          <rect x="60" y="154" width="15" height="28" rx="4" class="fill-gray-200 dark:fill-gray-600/50"/>
          <text x="52" y="173" text-anchor="end" font-size="12" font-family="ui-monospace, monospace" class="fill-gray-600 dark:fill-gray-400">known</text>
          <text x="83" y="173" font-size="11" font-family="system-ui" class="fill-gray-500 dark:fill-gray-400">3%</text>
        </g>

        {/* Annotation */}
        <text x="420" y="60" font-size="11" font-family="system-ui" class="fill-gray-500 dark:fill-gray-400">← remaining 18% spread across</text>
        <text x="432" y="76" font-size="11" font-family="system-ui" class="fill-gray-500 dark:fill-gray-400">~99,995 other tokens</text>
      </svg>
    </div>
  </div>
</div>

The model samples from this distribution to pick the next token, then feeds it back in and repeats. This is all it does — token in, probability distribution out, sample, repeat. Training adjusts billions of parameters so these distributions get better and better at matching what actually comes next in real text. This runs over months on thousands of GPUs, processing trillions of tokens. The model sees no labels, no instructions, no notion of right or wrong. It just learns which tokens are likely to follow which other tokens.

### Context Windows — How Much the Model Can See

There's a critical constraint: the model can only attend to a fixed number of tokens at a time. This is the **context window** — GPT-3 had 2,048 tokens, GPT-4 pushed to 128,000, and some models now support over a million. Everything the model needs to work with — the system prompt, the conversation history, the document you pasted in — must fit inside this window. If a conversation grows too long, the oldest messages get dropped.

But the window size isn't the whole story. Research on ["Lost in the Middle"](https://arxiv.org/abs/2307.03172) (Liu et al., 2023) found that models pay the most attention to information at the **beginning** and **end** of their context window, while information buried in the middle gets partially ignored. It's like reading a long report — you remember the opening and the conclusion, but the details on page 47 blur together.

<div class="not-prose my-8">
  <div class="rounded-xl border border-gray-200 dark:border-gray-700 overflow-hidden">
    <div class="px-5 py-2.5 bg-gray-50 dark:bg-gray-800/50 border-b border-gray-200 dark:border-gray-700 font-sans text-xs font-medium uppercase tracking-wider text-gray-500 dark:text-gray-400">Lost in the Middle — attention across the context window</div>
    <div class="px-4 py-6 sm:px-8 sm:py-8">
      <svg viewBox="0 0 700 140" class="w-full" aria-label="Diagram showing that models attend strongly to the beginning and end of the context window but poorly to the middle, forming a U-shaped attention curve.">
        {/* Axis */}
        <line x1="60" y1="100" x2="660" y2="100" class="stroke-gray-300 dark:stroke-gray-600" stroke-width="1.5"/>
        <text x="360" y="132" text-anchor="middle" font-size="11" font-family="system-ui" class="fill-gray-500 dark:fill-gray-400">Position in context window</text>
        <text x="60" y="132" text-anchor="middle" font-size="10" font-family="system-ui" class="fill-gray-400 dark:fill-gray-500">Start</text>
        <text x="660" y="132" text-anchor="middle" font-size="10" font-family="system-ui" class="fill-gray-400 dark:fill-gray-500">End</text>

        {/* U-shaped attention curve */}
        <path d="M 60 25 C 120 25, 180 85, 280 90 C 380 95, 420 95, 460 90 C 540 85, 600 25, 660 20" fill="none" class="stroke-blue-500 dark:stroke-blue-400" stroke-width="2.5"/>

        {/* Fill under curve */}
        <path d="M 60 25 C 120 25, 180 85, 280 90 C 380 95, 420 95, 460 90 C 540 85, 600 25, 660 20 L 660 100 L 60 100 Z" class="fill-blue-100/50 dark:fill-blue-900/20"/>

        {/* Labels */}
        <text x="90" y="18" font-size="10" font-family="system-ui" font-weight="600" class="fill-emerald-600 dark:fill-emerald-400">High attention</text>
        <text x="320" y="82" font-size="10" font-family="system-ui" font-weight="600" class="fill-red-500 dark:fill-red-400">Low attention</text>
        <text x="600" y="14" font-size="10" font-family="system-ui" font-weight="600" class="fill-emerald-600 dark:fill-emerald-400">High attention</text>

        {/* Y-axis label */}
        <text x="30" y="60" text-anchor="middle" font-size="10" font-family="system-ui" class="fill-gray-400 dark:fill-gray-500" transform="rotate(-90, 30, 60)">Attention</text>
      </svg>
    </div>
    <div class="px-5 py-3 bg-gray-50 dark:bg-gray-800/50 border-t border-gray-200 dark:border-gray-700 text-xs text-gray-500 dark:text-gray-400">
      This is why putting important instructions at the start of a prompt (system message) and at the end (the user's question) tends to work better than burying key details in the middle of a long document.
    </div>
  </div>
</div>

This has practical implications. If you paste a 50-page document and ask a question about a detail on page 30, the model may miss it — not because it can't process 50 pages, but because attention fades in the middle. It's one reason why retrieval-augmented generation (RAG) works well: instead of dumping everything into the context window, you retrieve only the relevant chunks and place them where the model will attend to them.

What you get at the end is a **base model** — essentially expensive autocomplete. It's a next-token probability machine, and nothing more.

This is genuinely hard to internalise, because the base model *feels* like it understands things. It can write fluent paragraphs, produce working code, and discuss philosophy. But all of these abilities are side effects of next-token prediction on internet-scale data. The model has no concept of being helpful, truthful, or safe. It's a mirror of the internet, and it behaves accordingly.

Here's what that actually looks like. If you give a base model a prompt like:

```
User: What is the capital of France?
Assistant:
```

You might expect it to reply "Paris." But a base model doesn't know it's supposed to *answer*. It's seen millions of web pages, and this text could be from anywhere — a forum post, a quiz, a dataset, a chatbot transcript. So it might:

- Complete it correctly: `The capital of France is Paris.`
- Generate more Q&A pairs: `User: What is the capital of Germany? User: What is the capital of Spain?` — because it's pattern-matching a list of questions
- Continue with something completely unrelated — whatever text statistically follows this kind of pattern in its training data
- Produce harmful or offensive content — it has no filter, because it was never taught what's appropriate

The base model is a powerful text generator with zero alignment. It can produce anything it saw in training, weighted by probability. Getting from here to a useful assistant requires the next three stages.

GPT-2 in 2019 cost roughly $40,000 to train. Karpathy later reproduced it for about $672 with modern tooling and hardware — a striking illustration of how fast the infrastructure has improved.

---

## Hallucinations — When Probability Beats Truth

Before we move to the stages that fix the base model, it's worth understanding the most important consequence of next-token prediction: **hallucination**. Because the model doesn't "know" things — it predicts likely tokens — it can produce confident, fluent, completely wrong output. Some of the most famous examples reveal exactly how this works.

### The Strawberry Problem

Ask an LLM: *"How many r's are in the word strawberry?"* For a long time, models confidently answered **two**. The correct answer is three.

<div class="not-prose my-8">
  <div class="rounded-xl border border-gray-200 dark:border-gray-700 overflow-hidden">
    <div class="px-5 py-2.5 bg-gray-50 dark:bg-gray-800/50 border-b border-gray-200 dark:border-gray-700 font-sans text-xs font-medium uppercase tracking-wider text-gray-500 dark:text-gray-400">Why "strawberry" breaks LLMs</div>
    <div class="px-4 py-6 sm:px-8 sm:py-8 space-y-6">
      <div>
        <div class="text-xs font-semibold uppercase tracking-wider text-gray-500 dark:text-gray-400 mb-3">What you see</div>
        <p class="text-2xl font-mono text-gray-800 dark:text-gray-200 tracking-wider">s t r a w b e r r y</p>
      </div>
      <div>
        <div class="text-xs font-semibold uppercase tracking-wider text-gray-500 dark:text-gray-400 mb-3">What the model sees</div>
        <div class="flex flex-wrap gap-1.5">
          <span class="px-2.5 py-1.5 rounded-md bg-blue-100 dark:bg-blue-900/40 text-blue-800 dark:text-blue-200 font-mono text-sm">st</span>
          <span class="px-2.5 py-1.5 rounded-md bg-emerald-100 dark:bg-emerald-900/40 text-emerald-800 dark:text-emerald-200 font-mono text-sm">raw</span>
          <span class="px-2.5 py-1.5 rounded-md bg-purple-100 dark:bg-purple-900/40 text-purple-800 dark:text-purple-200 font-mono text-sm">berry</span>
        </div>
      </div>
      <div class="rounded-lg bg-red-50 dark:bg-red-900/20 border border-red-200 dark:border-red-800 p-4">
        <p class="text-sm text-red-800 dark:text-red-200"><strong>The model never sees individual letters.</strong> "strawberry" is split into tokens like "st", "raw", "berry" — and the r's are buried across token boundaries. The model can't count characters it doesn't have access to. It's not bad at counting — it literally can't see what it's being asked to count.</p>
      </div>
    </div>
  </div>
</div>

This isn't a reasoning failure — it's an architectural one. The model operates on tokens, not characters. Asking it to count letters is like asking someone to count the bricks in a wall from a satellite photo. The resolution isn't there.

### The 9.11 vs 9.8 Problem

Ask an LLM: *"Which is larger, 9.11 or 9.8?"* Models frequently answer **9.11** — which is wrong. 9.8 is larger.

<div class="not-prose my-8">
  <div class="rounded-xl border border-gray-200 dark:border-gray-700 overflow-hidden">
    <div class="px-5 py-2.5 bg-gray-50 dark:bg-gray-800/50 border-b border-gray-200 dark:border-gray-700 font-sans text-xs font-medium uppercase tracking-wider text-gray-500 dark:text-gray-400">Why LLMs get 9.11 vs 9.8 wrong</div>
    <div class="px-4 py-6 sm:px-8 sm:py-8 space-y-5">
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
        <div class="rounded-lg border border-red-200 dark:border-red-800 bg-red-50 dark:bg-red-900/20 p-4">
          <div class="text-xs font-semibold uppercase tracking-wider text-red-600 dark:text-red-400 mb-2">How the model "thinks"</div>
          <p class="text-sm text-red-800 dark:text-red-200">9.11 has more digits after the decimal → 11 > 8 → therefore 9.11 > 9.8</p>
          <p class="text-xs text-red-600 dark:text-red-400 mt-2 font-semibold">Wrong — treating decimals like version numbers</p>
        </div>
        <div class="rounded-lg border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-900/20 p-4">
          <div class="text-xs font-semibold uppercase tracking-wider text-emerald-600 dark:text-emerald-400 mb-2">Actual math</div>
          <p class="text-sm text-emerald-800 dark:text-emerald-200">9.11 = 9 + 0.11<br/>9.8 = 9 + 0.80<br/>0.80 > 0.11 → 9.8 > 9.11</p>
          <p class="text-xs text-emerald-600 dark:text-emerald-400 mt-2 font-semibold">Correct — comparing decimal values</p>
        </div>
      </div>
      <p class="text-sm text-gray-600 dark:text-gray-400">The model has seen "9.11" thousands of times in its training data — mostly referring to <strong>September 11th</strong>, not the number nine-point-one-one. Its statistical associations with "9.11" are dominated by a date, not a decimal. It's also seen version numbers (Python 3.11 > 3.8) where more digits after the dot <em>does</em> mean larger. The model isn't doing maths — it's pattern-matching, and the patterns point the wrong way.</p>
    </div>
  </div>
</div>

### The Bible Verse Problem

This one goes deeper than it first appears. Researchers at [Transluce](https://transluce.org/observability-interface) — a nonprofit AI interpretability lab co-founded by Jacob Steinhardt (UC Berkeley) and Sarah Schwettmann (MIT) — used mechanistic interpretability tools to look *inside* the model and find out exactly why it gets this wrong. What they discovered: specific neurons fire when the model encounters "9.11" and "9.8" — and those neurons have nothing to do with decimal maths. They found **"Bible verse" neurons** trained on biblical text where chapter:verse notation (Luke 9:8 comes before Luke 9:11) treats the number after the colon as a sequential integer. They found **date neurons** where 9/11 comes after 9/8. And they found **gravity neurons** where 9.8 m/s² activates unrelated physics context.

When they tested 1,280 queries comparing numbers in this format, the baseline model got just 55% right — barely better than a coin flip. When they deactivated the Bible verse, date, and gravity neurons, accuracy jumped to 77%. Roughly 0.4% of all neurons in the model relate to the Bible — a tiny fraction, but enough to corrupt decimal comparisons across the board.

<div class="not-prose my-8">
  <div class="rounded-xl border border-gray-200 dark:border-gray-700 overflow-hidden">
    <div class="px-5 py-2.5 bg-gray-50 dark:bg-gray-800/50 border-b border-gray-200 dark:border-gray-700 font-sans text-xs font-medium uppercase tracking-wider text-gray-500 dark:text-gray-400">The pattern — why the model is "confidently wrong"</div>
    <div class="px-4 py-6 sm:px-8 sm:py-8">
      <svg viewBox="0 0 700 180" class="w-full" aria-label="Diagram showing three contexts where 9.11 appears to be greater than 9.8: Bible verses, software versions, and dates — all misleading the model for numerical comparison.">
        {/* Header */}
        <text x="350" y="20" text-anchor="middle" font-size="12" font-family="system-ui" font-weight="600" class="fill-gray-700 dark:fill-gray-300">Training data contexts for "9.11" and "9.8"</text>

        {/* Bible */}
        <rect x="20" y="40" width="200" height="85" rx="10" class="fill-amber-50 dark:fill-amber-900/20 stroke-amber-300 dark:stroke-amber-700" stroke-width="1.5"/>
        <text x="120" y="62" text-anchor="middle" font-size="11" font-family="system-ui" font-weight="600" class="fill-amber-700 dark:fill-amber-300">Bible verses</text>
        <text x="120" y="82" text-anchor="middle" font-size="10" font-family="ui-monospace, monospace" class="fill-amber-800 dark:fill-amber-200">Luke 9:8 ... Luke 9:11</text>
        <text x="120" y="98" text-anchor="middle" font-size="10" font-family="system-ui" class="fill-amber-600 dark:fill-amber-400">9:11 comes after 9:8</text>
        <text x="120" y="116" text-anchor="middle" font-size="11" font-family="system-ui" font-weight="700" class="fill-amber-700 dark:fill-amber-300">→ "9.11 > 9.8"</text>

        {/* Versions */}
        <rect x="250" y="40" width="200" height="85" rx="10" class="fill-blue-50 dark:fill-blue-900/20 stroke-blue-300 dark:stroke-blue-700" stroke-width="1.5"/>
        <text x="350" y="62" text-anchor="middle" font-size="11" font-family="system-ui" font-weight="600" class="fill-blue-700 dark:fill-blue-300">Software versions</text>
        <text x="350" y="82" text-anchor="middle" font-size="10" font-family="ui-monospace, monospace" class="fill-blue-800 dark:fill-blue-200">Python 3.8 → 3.11</text>
        <text x="350" y="98" text-anchor="middle" font-size="10" font-family="system-ui" class="fill-blue-600 dark:fill-blue-400">3.11 is newer than 3.8</text>
        <text x="350" y="116" text-anchor="middle" font-size="11" font-family="system-ui" font-weight="700" class="fill-blue-700 dark:fill-blue-300">→ "9.11 > 9.8"</text>

        {/* Dates */}
        <rect x="480" y="40" width="200" height="85" rx="10" class="fill-purple-50 dark:fill-purple-900/20 stroke-purple-300 dark:stroke-purple-700" stroke-width="1.5"/>
        <text x="580" y="62" text-anchor="middle" font-size="11" font-family="system-ui" font-weight="600" class="fill-purple-700 dark:fill-purple-300">Dates</text>
        <text x="580" y="82" text-anchor="middle" font-size="10" font-family="ui-monospace, monospace" class="fill-purple-800 dark:fill-purple-200">Sept 8 → Sept 11</text>
        <text x="580" y="98" text-anchor="middle" font-size="10" font-family="system-ui" class="fill-purple-600 dark:fill-purple-400">9/11 comes after 9/8</text>
        <text x="580" y="116" text-anchor="middle" font-size="11" font-family="system-ui" font-weight="700" class="fill-purple-700 dark:fill-purple-300">→ "9.11 > 9.8"</text>

        {/* Bottom conclusion */}
        <text x="350" y="155" text-anchor="middle" font-size="11" font-family="system-ui" class="fill-red-600 dark:fill-red-400">All three patterns reinforce the wrong answer. The model has almost no training data</text>
        <text x="350" y="172" text-anchor="middle" font-size="11" font-family="system-ui" class="fill-red-600 dark:fill-red-400">where "9.11" and "9.8" are compared as decimal numbers — because humans rarely write that comparison.</text>
      </svg>
    </div>
  </div>
</div>

### Brilliantly Smart, Bafflingly Stupid

These examples reveal something genuinely strange about LLMs: they are simultaneously the most capable and the most unreliable tools you'll ever use. The same model that can write a working compiler from a natural language description will confidently tell you that "strawberry" has two r's. The same model that can explain quantum entanglement clearly enough for a teenager to understand will insist that 9.11 is greater than 9.8. Karpathy calls this **"jagged intelligence"** — the model's capabilities aren't a smooth gradient from easy to hard. It can solve problems that stump most humans while failing tasks a child could do.

This isn't a bug that will be fixed in the next version. It's a structural consequence of how these systems work. The model doesn't *know* things — it has statistical patterns that usually correlate with truth. When the patterns align, it's astonishingly capable. When they don't, it's astonishingly wrong — and it has no way to tell the difference. It will deliver both with identical confidence.

The practical takeaway: **always cross-check**. Treat an LLM like a brilliant but unreliable colleague. Use it to draft, explore, brainstorm, and accelerate your work — but verify anything that matters. The moment you stop checking is the moment you'll get burned by a hallucination you didn't catch. These are tools that make you dramatically more productive *when you stay in the loop*. They're dangerous when you don't.

This is precisely why the remaining three training stages exist. SFT teaches models to say "I don't know" and to call tools (a calculator, a code interpreter) instead of guessing. RLHF punishes confident wrong answers. And RLVR, as we'll see, teaches the model to actually reason its way to verifiable answers.

---

## Stage 2: Supervised Fine-Tuning — Learning to Be Helpful

As we saw, the base model doesn't know it's supposed to be an assistant. It just predicts the next token. Supervised fine-tuning (SFT) fixes this by training the model on curated datasets of conversations — human-written examples of what a good assistant response looks like.

These datasets come from two sources. Human-curated collections like [OASST1](https://huggingface.co/datasets/OpenAssistant/oasst1) contain thousands of real conversations written by annotators. Synthetic datasets like [UltraChat](https://github.com/thunlp/UltraChat) scale this up to 1.5 million conversations generated by models and filtered for quality.

### Special Tokens — Teaching the Model Its Role

The key mechanism is **special tokens**. The raw training data for SFT isn't just plain text — it's structured with markers that tell the model who's speaking and when each turn starts and ends. These tokens don't appear in normal text. They're added to the vocabulary specifically for conversations.

<div class="not-prose my-8">
  <div class="rounded-xl border border-gray-200 dark:border-gray-700 overflow-hidden">
    <div class="px-5 py-2.5 bg-gray-50 dark:bg-gray-800/50 border-b border-gray-200 dark:border-gray-700 font-sans text-xs font-medium uppercase tracking-wider text-gray-500 dark:text-gray-400">SFT training data — what the model actually sees</div>
    <div class="px-4 py-6 sm:px-8 sm:py-8 font-mono text-sm space-y-0 leading-relaxed">
      <div class="flex items-start gap-2">
        <span class="px-2 py-0.5 rounded bg-purple-100 dark:bg-purple-900/40 text-purple-700 dark:text-purple-300 text-xs font-bold whitespace-nowrap shrink-0">special</span>
        <span class="text-purple-600 dark:text-purple-400">&lt;|im_start|&gt;system</span>
      </div>
      <div class="pl-16 text-gray-600 dark:text-gray-400 py-1">You are a helpful assistant.<br/>You answer questions accurately and concisely.</div>
      <div class="flex items-start gap-2">
        <span class="px-2 py-0.5 rounded bg-purple-100 dark:bg-purple-900/40 text-purple-700 dark:text-purple-300 text-xs font-bold whitespace-nowrap shrink-0">special</span>
        <span class="text-purple-600 dark:text-purple-400">&lt;|im_end|&gt;</span>
      </div>
      <div class="h-2"></div>
      <div class="flex items-start gap-2">
        <span class="px-2 py-0.5 rounded bg-blue-100 dark:bg-blue-900/40 text-blue-700 dark:text-blue-300 text-xs font-bold whitespace-nowrap shrink-0">special</span>
        <span class="text-blue-600 dark:text-blue-400">&lt;|im_start|&gt;user</span>
      </div>
      <div class="pl-16 text-gray-800 dark:text-gray-200 py-1">What causes the northern lights?</div>
      <div class="flex items-start gap-2">
        <span class="px-2 py-0.5 rounded bg-blue-100 dark:bg-blue-900/40 text-blue-700 dark:text-blue-300 text-xs font-bold whitespace-nowrap shrink-0">special</span>
        <span class="text-blue-600 dark:text-blue-400">&lt;|im_end|&gt;</span>
      </div>
      <div class="h-2"></div>
      <div class="flex items-start gap-2">
        <span class="px-2 py-0.5 rounded bg-emerald-100 dark:bg-emerald-900/40 text-emerald-700 dark:text-emerald-300 text-xs font-bold whitespace-nowrap shrink-0">special</span>
        <span class="text-emerald-600 dark:text-emerald-400">&lt;|im_start|&gt;assistant</span>
      </div>
      <div class="pl-16 text-gray-800 dark:text-gray-200 py-1">The northern lights (aurora borealis) are caused by charged particles from the sun colliding with gases in Earth's atmosphere...</div>
      <div class="flex items-start gap-2">
        <span class="px-2 py-0.5 rounded bg-emerald-100 dark:bg-emerald-900/40 text-emerald-700 dark:text-emerald-300 text-xs font-bold whitespace-nowrap shrink-0">special</span>
        <span class="text-emerald-600 dark:text-emerald-400">&lt;|im_end|&gt;</span>
      </div>
    </div>
    <div class="px-5 py-3 bg-gray-50 dark:bg-gray-800/50 border-t border-gray-200 dark:border-gray-700 text-xs text-gray-500 dark:text-gray-400">
      The purple tokens don't exist in normal text — they're added to the vocabulary specifically for SFT. The model learns: when I see <code class="text-xs">&lt;|im_start|&gt;assistant</code>, I should produce a helpful response until <code class="text-xs">&lt;|im_end|&gt;</code>.
    </div>
  </div>
</div>

After thousands of these examples, the model internalises the pattern: when special tokens indicate it's the assistant's turn, it should provide a helpful, accurate response. The special tokens are the mechanism — they give the model a structured "role" within a conversation.

### Tool Use — Learning to Call Experts

SFT also teaches the model to use **tools**. The training data includes examples where the assistant, instead of guessing, calls an external system — a search engine, a calculator, a code interpreter — and uses the result in its response.

<div class="not-prose my-8">
  <div class="rounded-xl border border-gray-200 dark:border-gray-700 overflow-hidden">
    <div class="px-5 py-2.5 bg-gray-50 dark:bg-gray-800/50 border-b border-gray-200 dark:border-gray-700 font-sans text-xs font-medium uppercase tracking-wider text-gray-500 dark:text-gray-400">Tool use — learned from SFT examples</div>
    <div class="px-4 py-6 sm:px-8 sm:py-8">
      <svg viewBox="0 0 700 240" class="w-full" aria-label="Diagram showing how a model learns to call tools during SFT: user asks a question, model calls a search tool instead of guessing, then uses the result to form an accurate answer.">
        {/* User question */}
        <rect x="10" y="10" width="200" height="50" rx="10" class="fill-blue-50 dark:fill-blue-900/30 stroke-blue-300 dark:stroke-blue-700" stroke-width="1.5"/>
        <text x="110" y="30" text-anchor="middle" class="fill-blue-600 dark:fill-blue-400" font-size="10" font-family="system-ui" font-weight="600">USER</text>
        <text x="110" y="46" text-anchor="middle" class="fill-blue-800 dark:fill-blue-200" font-size="11" font-family="system-ui">"Who won the 2024 Super Bowl?"</text>

        {/* Arrow to model */}
        <line x1="210" y1="35" x2="260" y2="35" class="stroke-gray-300 dark:stroke-gray-600" stroke-width="1.5" marker-end="url(#arrow2)"/>

        {/* Model decides */}
        <rect x="260" y="10" width="160" height="50" rx="10" class="fill-gray-100 dark:fill-gray-800 stroke-gray-300 dark:stroke-gray-600" stroke-width="1.5"/>
        <text x="340" y="30" text-anchor="middle" class="fill-gray-500 dark:fill-gray-400" font-size="10" font-family="system-ui" font-weight="600">MODEL THINKS</text>
        <text x="340" y="46" text-anchor="middle" class="fill-gray-700 dark:fill-gray-300" font-size="11" font-family="system-ui">"I should search for this"</text>

        {/* Arrow down to tool call */}
        <line x1="340" y1="60" x2="340" y2="90" class="stroke-gray-300 dark:stroke-gray-600" stroke-width="1.5" marker-end="url(#arrow2)"/>

        {/* Tool call */}
        <rect x="240" y="90" width="200" height="50" rx="10" class="fill-orange-50 dark:fill-orange-900/30 stroke-orange-300 dark:stroke-orange-700" stroke-width="1.5"/>
        <text x="340" y="110" text-anchor="middle" class="fill-orange-600 dark:fill-orange-400" font-size="10" font-family="system-ui" font-weight="600">TOOL CALL</text>
        <text x="340" y="126" text-anchor="middle" class="fill-orange-800 dark:fill-orange-200" font-size="10" font-family="ui-monospace, monospace">search("2024 Super Bowl winner")</text>

        {/* Arrow to tool result */}
        <line x1="440" y1="115" x2="490" y2="115" class="stroke-gray-300 dark:stroke-gray-600" stroke-width="1.5" marker-end="url(#arrow2)"/>

        {/* Tool result */}
        <rect x="490" y="90" width="200" height="50" rx="10" class="fill-yellow-50 dark:fill-yellow-900/30 stroke-yellow-300 dark:stroke-yellow-700" stroke-width="1.5"/>
        <text x="590" y="110" text-anchor="middle" class="fill-yellow-600 dark:fill-yellow-400" font-size="10" font-family="system-ui" font-weight="600">TOOL RESULT</text>
        <text x="590" y="126" text-anchor="middle" class="fill-yellow-800 dark:fill-yellow-200" font-size="11" font-family="system-ui">"Kansas City Chiefs defeated..."</text>

        {/* Arrow down to response */}
        <line x1="500" y1="140" x2="400" y2="175" class="stroke-gray-300 dark:stroke-gray-600" stroke-width="1.5" marker-end="url(#arrow2)"/>

        {/* Final response */}
        <rect x="200" y="170" width="300" height="55" rx="10" class="fill-emerald-50 dark:fill-emerald-900/30 stroke-emerald-300 dark:stroke-emerald-700" stroke-width="1.5"/>
        <text x="350" y="190" text-anchor="middle" class="fill-emerald-600 dark:fill-emerald-400" font-size="10" font-family="system-ui" font-weight="600">ASSISTANT RESPONSE</text>
        <text x="350" y="210" text-anchor="middle" class="fill-emerald-800 dark:fill-emerald-200" font-size="11" font-family="system-ui">"The Kansas City Chiefs won Super Bowl LVIII,</text>
        <text x="350" y="222" text-anchor="middle" class="fill-emerald-800 dark:fill-emerald-200" font-size="11" font-family="system-ui">defeating the San Francisco 49ers 25–22."</text>

        <defs>
          <marker id="arrow2" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
            <path d="M 0 0 L 10 5 L 0 10 z" class="fill-gray-400 dark:fill-gray-500"/>
          </marker>
        </defs>
      </svg>
    </div>
    <div class="px-5 py-3 bg-gray-50 dark:bg-gray-800/50 border-t border-gray-200 dark:border-gray-700 text-xs text-gray-500 dark:text-gray-400">
      The model wasn't born knowing how to call tools. It learned this pattern from SFT training examples that demonstrated: <strong>when unsure, call an expert instead of guessing.</strong>
    </div>
  </div>
</div>

This is a crucial capability. The base model would have either guessed (potentially wrong) or hallucinated a confident-sounding answer. The SFT-trained model has learned a different pattern from its training data: recognise when you don't know something, call the right tool, and use verified information to respond. Search, code execution, calculators — the model learns when to reach for each one.

The effect is striking. Take our earlier example:

```
User: What is the capital of France?
```

Before SFT, the base model might generate more questions, continue a document, or produce anything statistically likely. After SFT, the model consistently responds:

```
Assistant: The capital of France is Paris.
```

It's learned from thousands of examples that when text is structured as a user question, the appropriate behaviour is to provide a direct, helpful answer. The same shift happens across every type of interaction. Ask for code, and it writes code instead of continuing a tutorial. Ask it to summarise, and it summarises instead of extending the text. Ask it something it doesn't know, and it says "I don't know" instead of fabricating an answer — because the training data included examples of that behaviour.

SFT also teaches tool use. The training data includes examples where the assistant calls a search engine instead of guessing at facts, or runs code to verify an answer. And it teaches step-by-step reasoning — showing work instead of jumping to conclusions. None of these behaviours were present in the base model. They're all learned from examples.

The economics are dramatic. Where pretraining takes months and costs millions, SFT takes hours or days and costs orders of magnitude less. The datasets are tiny compared to pretraining data — thousands or millions of examples versus trillions of tokens. But the behavioural shift is enormous. Think of it like learning to cook by studying recipes and practising writing them out. You get good at reproducing known dishes — following established patterns reliably.

---

## Stage 3: RLHF — Learning from Human Preferences

SFT gets you a helpful assistant, but "helpful" is subjective. Ask a model to explain quantum mechanics and there are dozens of valid approaches — some clearer, some more accurate, some better matched to the audience. Is this summary good? Is this creative writing engaging? Is this joke funny? There's no formula for "better" — it requires human judgment.

### The Evaluation Problem

This is harder than it sounds. Consider: how do you automatically evaluate whether a joke is funny? You can't write a function for it. You can't check it against an answer key. Humour is subjective, cultural, contextual — the same joke lands differently depending on the audience. The same is true for writing quality, explanation clarity, conversational tone, and most of the things that separate a good AI assistant from a mediocre one.

You could try asking another AI to evaluate — but that just moves the problem. How do you evaluate the evaluator? This is the fundamental challenge: **evaluation of open-ended tasks is itself an open-ended task**.

### Why Ranking Works (For Humans)

Here's the clever insight behind RLHF: while *scoring* quality on an absolute scale is nearly impossible, *ranking* is something humans do naturally and reliably. Show someone two responses and ask "which is better?" — they can almost always tell you, even if they can't articulate a precise score. Consider this example:

<div class="not-prose my-8">
  <div class="rounded-xl border border-gray-200 dark:border-gray-700 overflow-hidden">
    <div class="px-5 py-2.5 bg-gray-50 dark:bg-gray-800/50 border-b border-gray-200 dark:border-gray-700 font-sans text-xs font-medium uppercase tracking-wider text-gray-500 dark:text-gray-400">RLHF — humans rank, they don't score</div>
    <div class="px-4 py-6 sm:px-8 sm:py-8 space-y-4">
      <div>
        <div class="text-xs font-semibold uppercase tracking-wider text-gray-500 dark:text-gray-400 mb-2">Prompt</div>
        <p class="text-sm font-mono text-gray-800 dark:text-gray-200">"Write a short poem about rain"</p>
      </div>
      <div class="grid grid-cols-1 sm:grid-cols-3 gap-3">
        <div class="rounded-lg border-2 border-emerald-300 dark:border-emerald-700 bg-emerald-50 dark:bg-emerald-900/20 p-4">
          <div class="text-xs font-bold text-emerald-600 dark:text-emerald-400 mb-2">Response A — Ranked #1</div>
          <p class="text-sm text-gray-700 dark:text-gray-300 italic">"The rain speaks in a language<br/>only gutters understand —<br/>running sentences together<br/>until the streets are fluent."</p>
        </div>
        <div class="rounded-lg border border-gray-200 dark:border-gray-700 bg-gray-50 dark:bg-gray-800/50 p-4">
          <div class="text-xs font-bold text-gray-500 dark:text-gray-400 mb-2">Response B — Ranked #2</div>
          <p class="text-sm text-gray-700 dark:text-gray-300 italic">"Rain falls from the sky,<br/>drops of water, wet and cold.<br/>Puddles on the ground."</p>
        </div>
        <div class="rounded-lg border border-red-200 dark:border-red-800 bg-red-50 dark:bg-red-900/20 p-4">
          <div class="text-xs font-bold text-red-500 dark:text-red-400 mb-2">Response C — Ranked #3</div>
          <p class="text-sm text-gray-700 dark:text-gray-300 italic">"The the the the rain<br/>is wet and also the the<br/>water falls down wet."</p>
        </div>
      </div>
      <p class="text-xs text-gray-500 dark:text-gray-400">You can't put a number on why A is better than B. But you <em>instantly</em> know C is worst and A is best. This is what RLHF exploits.</p>
    </div>
  </div>
</div>

Reinforcement Learning from Human Feedback (RLHF) uses this insight. The model generates multiple responses to the same prompt, human annotators rank them from best to worst, and a **reward model** is trained to approximate those rankings. Once trained, the reward model can score any response — scaling evaluation from a few thousand human judgments to millions of model outputs. The language model is then optimised via reinforcement learning to produce responses that score higher on the reward model.

This works well for subjective quality — tone, clarity, helpfulness, safety. The early iterations produce genuine improvements. Responses get more natural, more careful, better structured. But there's a fundamental problem: the reward model is an approximation, not ground truth. It's a neural network trained to *simulate* human preferences, and it has blind spots.

### Reward Hacking — When the Model Games the System

Given enough optimisation pressure, models learn to exploit those blind spots. This is called **reward hacking**. The model discovers patterns that score highly on the reward model but aren't actually better by human standards.

<div class="not-prose my-8">
  <div class="rounded-xl border border-gray-200 dark:border-gray-700 overflow-hidden">
    <div class="px-5 py-2.5 bg-gray-50 dark:bg-gray-800/50 border-b border-gray-200 dark:border-gray-700 font-sans text-xs font-medium uppercase tracking-wider text-gray-500 dark:text-gray-400">Reward hacking — how the model games RLHF</div>
    <div class="px-4 py-6 sm:px-8 sm:py-8">
      <svg viewBox="0 0 700 200" class="w-full" aria-label="Chart showing model quality improving in early RLHF steps then degrading as reward hacking takes over, with a dotted line showing where training should be cut off.">
        {/* Axes */}
        <line x1="60" y1="170" x2="660" y2="170" class="stroke-gray-300 dark:stroke-gray-600" stroke-width="1.5"/>
        <line x1="60" y1="170" x2="60" y2="20" class="stroke-gray-300 dark:stroke-gray-600" stroke-width="1.5"/>
        <text x="360" y="198" text-anchor="middle" font-size="11" font-family="system-ui" class="fill-gray-500 dark:fill-gray-400">RL training steps</text>
        <text x="20" y="95" text-anchor="middle" font-size="11" font-family="system-ui" class="fill-gray-500 dark:fill-gray-400" transform="rotate(-90, 20, 95)">Quality</text>

        {/* Reward model score - keeps going up */}
        <path d="M 80 150 C 150 130, 200 100, 280 70 C 360 45, 450 30, 640 15" fill="none" class="stroke-blue-400 dark:stroke-blue-500" stroke-width="2" stroke-dasharray="6,4"/>
        <text x="645" y="12" font-size="9" font-family="system-ui" class="fill-blue-500 dark:fill-blue-400">Reward model score</text>

        {/* Actual quality - goes up then down */}
        <path d="M 80 150 C 150 125, 200 90, 300 55 C 380 65, 450 85, 640 130" fill="none" class="stroke-emerald-500 dark:stroke-emerald-400" stroke-width="2.5"/>
        <text x="645" y="134" font-size="9" font-family="system-ui" class="fill-emerald-600 dark:fill-emerald-400">Actual quality</text>

        {/* Cut-off line */}
        <line x1="300" y1="25" x2="300" y2="165" class="stroke-red-400 dark:stroke-red-500" stroke-width="1.5" stroke-dasharray="4,3"/>
        <text x="305" y="38" font-size="10" font-family="system-ui" font-weight="600" class="fill-red-500 dark:fill-red-400">← cut off here</text>
        <text x="305" y="52" font-size="9" font-family="system-ui" class="fill-red-400 dark:fill-red-500">(~100-1000 steps)</text>

        {/* Annotations */}
        <text x="160" y="108" font-size="9" font-family="system-ui" class="fill-emerald-600 dark:fill-emerald-400">Genuine</text>
        <text x="152" y="120" font-size="9" font-family="system-ui" class="fill-emerald-600 dark:fill-emerald-400">improvement</text>
        <text x="470" y="96" font-size="9" font-family="system-ui" class="fill-red-500 dark:fill-red-400">Reward hacking:</text>
        <text x="470" y="108" font-size="9" font-family="system-ui" class="fill-red-500 dark:fill-red-400">score ↑ quality ↓</text>
      </svg>
    </div>
  </div>
</div>

It might learn that longer responses score higher (because annotators sometimes equate length with thoroughness), so it becomes verbose. It might learn that hedging language ("It's important to note that...") scores well, so it hedges everything. It might learn to produce confident-sounding but shallow answers that pattern-match what the reward model likes. The model isn't trying to be helpful — it's trying to maximise a score, and maximising a score is not the same thing.

This is why Karpathy notes that training caps are essential — typically 100 to 1,000 RL steps. Early in RLHF training, the reward model's signal is genuinely useful and the model improves. But after too many steps, the model starts overfitting to the reward model's imperfections rather than actual quality. You have to cut it off before the gaming overtakes the gains. RLHF gets you meaningful improvements, but it has a ceiling — and pushing past that ceiling makes things worse, not better.

---

## Stage 4: RLVR — The 2025 Paradigm Shift

In his [2025 LLM Year in Review](https://karpathy.bearblog.dev/year-in-review-2025/), Karpathy identifies Reinforcement Learning with Verifiable Rewards (RLVR) as the single biggest paradigm change of the year. It's the breakthrough that turned LLMs from sophisticated text predictors into systems that can genuinely reason.

The key insight: for tasks with **verifiable answers** — math problems, coding challenges, logic puzzles — you don't need humans in the loop at all. The model generates an attempt, an automated verifier checks whether the answer is correct, and reinforcement learning optimises toward correct solutions. No human annotators. No imperfect reward models. Just a binary signal: right or wrong.

### DeepSeek-R1 — When the Secret Got Out

What makes RLVR remarkable isn't the mechanism — it's what emerges. Models trained with RLVR spontaneously develop reasoning strategies that nobody explicitly taught them. They start breaking problems into steps. They self-correct mid-reasoning. They explore alternative approaches when their first attempt fails. These behaviours weren't in the training data and weren't programmed — they emerged because the reward signal (correctness) incentivised whatever internal strategies produced right answers.

For years, the specifics of how RL was applied to language models were closely guarded. OpenAI's o1 model demonstrated remarkable reasoning capabilities but the company published almost nothing about how it was trained — no paper, no technical details, just the product. The techniques were widely assumed to exist but remained behind closed doors at a handful of well-funded labs.

Then, in January 2025, DeepSeek — a Chinese AI lab — published the [DeepSeek-R1 paper](https://arxiv.org/abs/2501.12948) and open-sourced the model weights. The paper laid out the full recipe: start with a base model, apply RLVR with verifiable rewards on maths and code, and watch reasoning emerge spontaneously. They showed the model developing chain-of-thought reasoning, self-verification, and backtracking — all without being explicitly trained on any of these strategies. Crucially, they published it all. The paper was a watershed moment: it demonstrated that RLVR wasn't some proprietary magic — it was a technique any sufficiently resourced team could replicate. It also showed that a relatively modest lab could produce reasoning capabilities competitive with models from labs spending orders of magnitude more. The genie was out of the bottle.

There's a direct parallel to [AlphaGo](https://www.youtube.com/watch?v=WXuK6gekU1Y). In its match against Lee Sedol, AlphaGo played Move 37 — a move no human Go player would have considered, discovered purely through self-play against a verifiable objective (winning the game). RLVR enables the same kind of emergent innovation in language. The model isn't constrained to strategies humans demonstrated — it discovers what works.

The cooking analogy makes this concrete:

> SFT is like learning to cook by reading recipes and practising writing them out. RLVR is like being given ingredients and a taste test at the end — you figure out what works through actual attempts. The first produces someone good at reproducing known recipes. The second can produce someone who discovers combinations nobody thought of, because the feedback loop rewards outcomes not process.

### Visible vs Hidden Reasoning

RLVR also created an interesting transparency divide. DeepSeek's open-source models show their full chain-of-thought reasoning — you can watch the model think step by step, backtrack, try alternative approaches, and arrive at an answer. It's like watching someone's working in the margin of an exam paper. This is invaluable for understanding *why* the model gave a particular answer and for catching errors mid-reasoning.

OpenAI's reasoning models (o1, o3) take the opposite approach: they generate reasoning traces internally but **hide them from the user**. You see a "thinking..." indicator and then get the final answer, but the actual chain of thought is redacted. The widely assumed reason is competitive: if the full reasoning traces were visible, competitors could study them to reverse-engineer OpenAI's RLVR training methodology — what reward signals they use, what reasoning patterns emerge, how the model structures its thinking. The reasoning traces are essentially a window into the proprietary training recipe. Anthropic takes a middle ground with Claude — showing a summarised version of the reasoning but not the raw trace.

This matters practically. When you can see the reasoning, you can verify it. When you can't, you're trusting a black box. It's another reason to cross-check: even when a model reasons its way to an answer, the reasoning itself can contain errors that are only visible if you can actually read it.

### Test-Time Compute — A New Scaling Axis

RLVR also unlocked a new scaling axis: **test-time compute**. Instead of making models bigger, you give them more thinking time at inference. Longer reasoning traces — more internal "steps" before producing an answer — lead to better answers. This is conceptually novel. Previously, the only way to make models smarter was to make them larger or train them on more data. Now you can make the same model smarter by letting it think longer.

Karpathy notes that compute budgets shifted significantly in 2025 — away from pretraining and toward RL. Models stayed similar sizes but got dramatically smarter through longer reinforcement learning training runs. The returns from RLVR haven't plateaued the way RLHF's did.

---

## The Full Pipeline

The four stages form a clear progression, each building on the last:

1. **Pretraining** → broad knowledge. The model absorbs the statistical structure of human language. An internet mirror.
2. **SFT** → helpful behaviour. The model learns to follow instructions and have conversations. Recipe following.
3. **RLHF** → refined taste. Human preferences polish the model's outputs for subjective quality. The soft edges get smoothed.
4. **RLVR** → genuine reasoning. Self-discovered strategies emerge from verifiable feedback. The model learns to think.

Each stage is faster and cheaper than the one before it. Pretraining takes months and costs millions. SFT takes days. RLHF and RLVR take days to weeks. But the later stages produce disproportionate capability gains — RLVR in particular has delivered the largest qualitative improvements in model reasoning despite using a fraction of the compute budget.

---

## Where This Goes

Karpathy's assessment is striking: "the industry has realized less than 10% of current capability potential." The models we're using today are early. The techniques — especially RLVR and test-time compute scaling — are still being explored. The ceiling hasn't been found yet.

If you want to go deeper, I keep a curated collection of resources on my [bookmarks page](/bookmarks) — papers, talks, and tools covering everything from transformer internals to evaluation frameworks.

---

## Questions?

If you're thinking about how LLMs work under the hood and want to talk through any of this — the training pipeline, RLVR, or how these ideas apply to what you're building — feel free to reach out.

You can find me at [rmccaffr@tcd.ie](mailto:rmccaffr@tcd.ie) or on [LinkedIn](https://linkedin.com/in/robbiemc8).
